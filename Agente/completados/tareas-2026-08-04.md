# Tareas completadas — 2026-08-04

## 028A-6 (SNT-10, Fase 1a) — Scope del orquestador extraído al core de Sentinel + submódulo `tools/sentinel`

- **Qué:** se inició la Fase 1 del plan 028A-6 (extraer el orquestador `scripts/quality` a Sentinel Core) con el primer módulo, y se completó la infraestructura de checkout interno para Sentinel:
  - `tools/sentinel` como submódulo interno (gitlink pin en main + `.gitmodules`), `quality-tools.json` con `sourcePath: "tools/sentinel"` y `quality:setup` reproducible en clon limpio. Con esto **el gate ya no necesita ninguna variable `GLORY_*`**: sentinel y varsense quedan fijados por gitlink; el lock no declara `sourcePathEnv`.
  - `src/core/scope.ts` (nuevo, upstream): port agnóstico de `scripts/quality/scope.mjs` — `resolveFullDecision` (requested/automatic/effective/execution), `matches`/globs, `resolveExplicitProfiles` (con `EXECUTABLE_PROFILES`), `expandLocalDependencies` y `detectScope` (git vía `execFile`, hashes, `changed-files.txt` + `scope-manifest.json`, escritura atómica y `mkdir` propio del `reportRoot`). Sin imports de wandori.us ni de VarSense (verificado por `check:core`).
  - `sentinel check <task-id> --dry-run` en `src/cli/index.ts`: calcula el alcance efectivo del workspace, escribe el manifiesto y devuelve JSON (incluye `taskId`); exige `--dry-run` porque el gate completo aún vive en `scripts/quality` (no se simula un orquestador inexistente). `quality.config.json` se consume como datos de transición (comentado: la fuente canónica será la política v2).
  - `src/test/suite/scope.test.ts`: 5 tests mocha (globs, perfiles, dependencias locales, matriz de decisión y `detectScope` sobre un repo git real con commit + archivo untracked).
- **Pruebas:** upstream — compile (tsc), `check:core` y **320 tests de la suite PASS** (5 nuevos de scope). Demo end-to-end: `sentinel check --dry-run` sobre un repo git temporal detecta el archivo nuevo, decide `incremental` y escribe el manifest. Gate — `quality:test` 136/136 PASS y `quality:setup` sin env vars verifica ambos tools.
- **Archivos:** upstream sentinel `src/core/scope.ts` (nuevo), `src/test/suite/scope.test.ts` (nuevo), `src/cli/index.ts`; gate `.gitmodules`, `tools/sentinel` (gitlink), `quality-tools.json` (`sourcePath` + commit `5a968c8`), `sentinel.lock.json`, `AGENTS.md`, `roadmap-sentinel.md`, plan 028A-6.
- **Revisión:** 3 hallazgos atendidos — `taskId` aceptado pero no usado (ahora se refleja en el JSON de salida), `detectScope` con footgun ENOENT si falta `reportRoot` (el core lo crea con `mkdir` propio), y `quality.config.json` sin marcar como fuente de transición (comentado). Quedó documentado que el task-id posicional solo se acepta en la primera posición.
- **Límites:** el gate completo sigue en `scripts/quality` (migración incremental); scheduler/cooldown, caché, reporter combinado, clasificador, `guard/doctor/status/install` y shims globales quedan para los siguientes pasos de la Fase 1. El submódulo de sentinel queda fijado en `5a968c8`; el trabajo de Fase 1 continúa en el mismo checkout interno.

## 028A-6 (SNT-10, Fase 1b) — Reporte combinado del orquestador extraído al core de Sentinel

- **Qué:** segundo módulo de la Fase 1 del plan 028A-6, después del scope:
  - `src/core/redaction.ts` (nuevo): port de `scripts/quality/redaction.mjs` — `redact` (assignments de TOKEN/KEY/SECRET/PASSWORD, Bearer, credenciales en URLs), `truncate` y `sanitize` recursivo por clave.
  - `src/core/gateReport.ts` (nuevo): port de `scripts/quality/reporter.mjs` — `createReport` (decisión PASS/FAIL/SETUP-ERROR/CANCELLED-130, alcance honesto requested/automatic/effective/execution, identidad de política, herramientas, etapas, hallazgos con orden estable sin locale, `latest.json` + `latest.md` con escritura atómica y sanitización), `compactLines`/`printCompact` (límites defensivos 3 hallazgos / 4 recordatorios). El comando recomendado del runtime agnóstico pasa a `sentinel check <task-id>` (antes `npm run task:check`), con `--full --allow-heavy` para el full diferido.
  - `src/test/suite/gateReport.test.ts`: 7 tests mocha (compacto con límites, secretos redactados en JSON/MD/compacto, orden determinista de hallazgos con artifact completo, cancelación 130, política en JSON/MD, full diferido con `heavy-deferred`).
- **Pruebas:** upstream — compile (tsc), `check:core` y **327 tests de la suite PASS** (7 nuevos). Gate — `quality:test` 136/136 (sin cambios en el orquestador local: el port es aditivo).
- **Archivos:** upstream sentinel `src/core/{redaction,gateReport}.ts` (nuevos), `src/test/suite/gateReport.test.ts` (nuevo); gate `tools/sentinel` (gitlink a `7eee63e`), `quality-tools.json`, `sentinel.lock.json`, plan 028A-6.
- **Límites:** scheduler/cooldown (`heavy-run-guard`), caché de etapas (`cache.mjs`), clasificador (`policy-decision`) y el resto del CLI global (guard/doctor/status/install + shims) siguen pendientes de la Fase 1; el gate de wandori.us sigue en `scripts/quality`.

## 028A-6 (SNT-10, Fase 1c) — Cache, scheduler y clasificador de política extraídos al core de Sentinel

- **Qué:** tercera tanda de la Fase 1 del plan 028A-6 — con esto los **cinco módulos puros del orquestador viven en Sentinel Core** (scope, reporter, cache, scheduler y clasificador), sin imports de wandori.us ni de VarSense (verificado por `check:core`):
  - `src/core/atomicFile.ts` (nuevo): `writeAtomic` pasa a utilidad compartida del core; `scope.ts` y `gateReport.ts` dejan sus copias inline y la importan.
  - `src/core/stageCache.ts` (nuevo): port de `scripts/quality/cache.mjs` — fingerprint v5 con runtime/plataforma/arch, modo por alcance efectivo (`ci/full/local-light`, incluido automaticFull permitido vs full diferido 028A-8), stage, `qualityConfig`, `toolManifest`, `policyHash` (o `policyIdentity`) y lock; `readCachedPass`/`writeCachedPass` con escritura atómica. Defensas añadidas frente al original: `?? null`/`?? []` (el original lanzaba con `hash.update(undefined)`).
  - `src/core/scheduler.ts` (nuevo): port de `heavy-run-guard.mjs` — `resolveTargetBase`/`resolveGuardRoot`, `findQualityRoot` con marcador declarativo `sentinel.config.json`/`quality.config.json` (lstat para omitir symlinks), `isHeavyCargoCommand`, `isHeavyOverride`, `inspectHeavyRun` (cooldown por `projectKey(projectRoot)`, override `--allow-heavy`/CI/env), `acquireHeavyRun` (lock `active.json` con flag `wx` + EEXIST, stale lock por PID) y `formatHeavyGuardMessage`. `readProjectConfig` fusiona `heavyRun` v2??v1 durante la migración (un cooldown v1 no se pierde cuando existe `sentinel.config.json` sin `heavyRun`). El wrapper `--execute-cargo` queda en el orquestador: es integración de migración, no capacidad del core.
  - `src/core/policyDecision.ts` (nuevo): port de `policy-decision.mjs` — `policyDecision` (no-policy/legacy-v1/policy/invalid-policy) y `decisionForGuard` (observe/enforce/pass-through con `blocked`/`observed`). Puro, sin I/O.
  - `src/cli/index.ts`: `sentinel check --full|--ci` consulta `inspectHeavyRun`; si el guard está en cooldown y no hay override, el alcance se detecta con `heavyDeferred` (efectivo local-light, `fullReason: heavy-deferred`) y la salida JSON expone `heavyGuard: { reason, nextAllowedAt }`. `--allow-heavy` documentado en usage.
  - Tests nuevos: `stageCache.test.ts` (3: hit/miss por contenido+formato, separación local/full/CI/policy/lock, alcance efectivo), `scheduler.test.ts` (7: cooldown+override, lock simultáneo, `findQualityRoot` físico vs symlink junction/file, fallback de ruta inexistente, comandos pesados, cooldown por proyecto con mismo targetBase, mensajes de guard) y `policyDecision.test.ts` (4).
- **Pruebas:** upstream — compile (tsc), `check:core` y **342 tests de la suite PASS** (14 nuevos). Demo end-to-end con guard root aislado: full sin cooldown → `effectiveFull: true`; con cooldown sembrado → `effectiveFull: false` + `heavyGuard: cooldown`; con `--allow-heavy` → full de nuevo; sin `--full` el guard no aplica. `npm run lint` del repo quedó interceptado por el guard del proyecto (validación directa bloqueada por diseño, exit del wrapper); la validación real es el gate del workspace.
- **Archivos:** upstream sentinel `src/core/{atomicFile,stageCache,scheduler,policyDecision}.ts` (nuevos), `src/core/{scope,gateReport}.ts` (usan `atomicFile`), `src/test/suite/{stageCache,scheduler,policyDecision}.test.ts` (nuevos), `src/cli/index.ts`; gate `tools/sentinel` (gitlink a `7f5b086`), `quality-tools.json`, `sentinel.lock.json`, `roadmap-sentinel.md`, plan 028A-6.
- **Revisión:** 4 hallazgos atendidos — precedencia de config (v2 sombreaba `heavyRun` v1; ahora se fusiona v2??v1), `writeAtomic` triplicado (scope/gateReport ahora importan `atomicFile`), cast `as never` en test (→ `as StageCacheScope`), ternario redundante en `policyDecision` (→ `action: mode`), y `formatHeavyGuardMessage` sin cobertura (nuevo test con cooldown/active/generic).
- **Límites:** runner de etapas e integración `check`→etapas→reporte, `guard/doctor/status/install/update/rollback` y shims globales siguen pendientes de la Fase 1; el gate de wandori.us sigue en `scripts/quality` (migración incremental, aditiva y reversible).

## 028A-6 (SNT-10, Fase 1d) — Runner de etapas, guard/doctor/status y `check` real con etapas declarativas

- **Qué:** cuarta tanda de la Fase 1 — con esto el orquestador completo vive en Sentinel Core y `sentinel check` ejecuta un gate real:
  - `src/core/toolRunner.ts` (nuevo): port de `runner.mjs` — `runProcess` con env allowlist, captura de stdout/stderr acotada a 64 KiB con marcador de truncado, timeout, cancelación cooperativa con sondeo y terminación de árbol (`taskkill /t /f` en Windows); `safeEnvironment`, `cancelAll`.
  - `src/core/stageRunner.ts` (nuevo): port de `stage-runner.mjs` — `runBoundedStages` con concurrencia limitada, orden determinista y drenaje de etapas activas ante error/cancelación (nunca agenda trabajo nuevo tras el primer fallo).
  - `src/core/structuredTool.ts` (nuevo): port de `adapters/structured-tool.mjs` + `common.mjs` — contrato de herramienta JSON versionado: `readToolReport` valida `schemaVersion`/`entries`/`findings` (ruleId/message/severidad allowlisted, fail-closed), `normalizeEntries`/`normalizeSeverity`, `toolFailure` con estados `tool-error/timeout/cancelled/invalid-output` y `resultFromFindings` (`fail` con findings error).
  - `src/core/guardCommand.ts` (nuevo): port de `quality-command-guard.mjs` — `inspectDirectCommand` (npm/npx/cargo/tools con defaults genéricos, política v2 `sentinel.config.json` con `guard.directCommands`, fallback legacy-v1, `GLORY_QUALITY_GATE_TOKEN` exime, exit 78) y `formatBlockMessage` recomendando `sentinel check <task-id>`. `hasQualityMarker` en `scheduler.ts` distingue raíz real de fallback (guard/doctor no mienten sobre la raíz).
  - `src/core/diagnose.ts` (nuevo): diagnóstico read-only de `doctor`/`status` — política (status/modo/hash), lock (`sentinel.lock.json`), versiones, scheduler (target/estado) y pins de submódulos vía `git ls-tree`; nunca muta estado.
  - `src/core/gateRun.ts` (nuevo): orquestación del gate (extraída del CLI para respetar el estándar de líneas y ser testeable) — `runCheck`: alcance → guard (full diferido) → caché de etapas (fingerprint con modo efectivo/política/lock/manifiesto) → `runBoundedStages` → `runStructuredTool` → `createReport`/`compactLines` con exit code PASS/FAIL/SETUP-ERROR/CANCELLED. `loadStages` valida el contrato declarativo (`timeoutMs` numérico, `{reportPath}` sustituido) y la identidad de caché hashea la política del workspace (edge case de raíz ancestro comentado).
  - `src/cli/index.ts`: comandos `guard` (con `--json`), `doctor`, `status`; `check` sin `--dry-run` requiere `--stages <json>` y ejecuta el gate real; `SIGINT/SIGTERM` → `cancelAll` en la ejecución real (sin huérfanos). El CLI queda en 504 líneas (el grueso restante es el plumbing de `analyze` preexistente, fuera del alcance de esta migración).
- **Pruebas:** upstream — compile (tsc), `check:core` y **371 tests de la suite PASS** (29 nuevos: toolRunner 7, stageRunner 4, structuredTool 3, guardCommand 8, diagnose 2, gateRun 4 con repos git reales). Demo end-to-end: `check DEMO-1 --stages` PASS con etapa ejecutada y reporte escrito; etapa con finding error → FAIL exit 1; `guard` sobre el workspace real → BLOQUEADO exit 78 (legacy-v1); `doctor`/`status` diagnostican política/lock/scheduler/submódulos (lock `7f5b0867`... en el momento del demo, ahora `a57cfc1`).
- **Archivos:** upstream sentinel `src/core/{toolRunner,stageRunner,structuredTool,guardCommand,diagnose,gateRun}.ts` (nuevos), `src/core/scheduler.ts` (hasQualityMarker), `src/cli/index.ts`, `src/test/suite/{toolRunner,stageRunner,structuredTool,guardCommand,diagnose,gateRun}.test.ts` (nuevos); gate `tools/sentinel` (gitlink a `a57cfc1`), `quality-tools.json`, `sentinel.lock.json`, `roadmap-sentinel.md`, plan 028A-6.
- **Revisión:** 6 hallazgos atendidos — `timeoutMs` NaN causaba timeout inmediato (validación fail-closed en `loadStages`), CLI por encima de 300 líneas (orquestación movida a `core/gateRun.ts`), hijos huérfanos en Ctrl+C (SIGINT/SIGTERM → `cancelAll`), raíz engañosa en doctor/guard sin marcador (`hasQualityMarker`), helper de rama duplicado (`currentBranch` exportado desde diagnose) e identidad de caché con raíz ancestro (comentado).
- **Límites:** `install/update/rollback` del runtime global y shims siguen pendientes de la Fase 1; la integración del orquestador de wandori.us (`scripts/quality`) a `sentinel check --stages` y la doble vía en observe quedan para la Fase 3; la partición de reportes por branch-key del core (`check/<task-id>` plano) se alineará con el orquestador cuando se integre.

## 028A-6 (SNT-10, Fase 1e) — Runtime global versionado `install/update/rollback` + supervisión automática de targets

- **Qué:** cierre de la Fase 1 — el runtime global del contrato de actualización/rollback vive en el core, y el gate de wandori.us deja de llenar el disco sin supervisión:
  - `src/core/runtimeInstall.ts` (nuevo, upstream): implementa `politica-actualizacion-rollback-sentinel-2026-08-04.md` — `installRuntime` con staging limpio por operación en `.tmp/<v>` (un crash previo no contamina el hash), hash sha256 del artefacto (`out/**` + package.json ± lock) calculado ANTES de escribir el manifest (no se contamina a sí mismo), `current.json` atómico vía `writeAtomic`, retiro no destructivo de la versión previa a `.retired/` antes del switch (sin ventana de pérdida: un crash entre renames deja `current.json` apuntando a una versión que existe), shims `current.js` (resuelve el CLI activo desde `current.json`) + `bin/sentinel.cmd`/`bin/sentinel`, `rollbackRuntime` a la anterior conservada o versión explícita con orden estable sin localeCompare, `runtimeStatus` con versiones/alias/hash verificado del artefacto instalado y `dry-run` que no escribe nada.
  - `src/cli/index.ts`: comandos `sentinel install|update|rollback` con `--target-root/--source-root/--version/--dry-run/--json` (install/update comparten `installRuntime`; rollback restaura la anterior). `src/core/diagnose.ts`: `doctor`/`status` ahora incluyen el estado del runtime (versiones, activa, hash verificado) para la verificación post-instalación del contrato.
  - **Supervisión automática de targets (orquestador, wandori.us):** `target-maintenance.mjs` endurecido — `runningProcessPaths()` (WMI) protege targets cuyo ejecutable está cargado por un proceso vivo, `pathSize` con presupuesto de tiempo (`truncated`) y lock entre agentes; la cuota estricta se comprueba en cada gate, preservando marcadores, procesos y escrituras recientes, pero podando históricos seguros aunque exista throttle de retención. `target-maintenance-stage.mjs` ejecuta el pase con budget de 60 s; `task-check.mjs` lo invoca tras la retención y `reporter.mjs` expone `targetMaintenance`/`quotaExceeded` en `latest.json`/`.md`. Si los targets activos solos superan la cuota, no se mata ningún proceso y el reporte lo deja explícito. `quality:cleanup` sigue forzando el pase completo.
- **Pruebas:** upstream — compile (tsc), `check:core` y **380 tests PASS** (11 nuevos: runtimeInstall 9 — install/hash/update conserva anterior/same-version sin EEXIST/rollback/sin anterior/dry-run/hash determinista — y 2 de parse CLI install/update/rollback). Demo end-to-end: install→update→rollback→shim real sobre target aislado (dry-run sin escribir, hash de 64 hex, versión anterior conservada, rollback atómico). Orquestador — `quality:test` **141/141 PASS** (5 nuevos de target-maintenance: cuota, proceso vivo por ruta, escritura reciente, dry-run, throttle + fallo no rompe best-effort). Revisión: 5 hallazgos atendidos — budget sin default (el primer pase colgaría el gate), cleanup manual sin rearmar throttle, escritura reciente protegida de la cuota, `targetRoot` engañoso en el wiring y dead imports. **Limpieza real del disco:** `C:\tmp` pasó de 2.1 GB libres (100%) a 31 GB libres (87%) podando `glory-target/debug` (11 GB, verificado sin procesos vivos) y `glory-target-codegen` (6 GB); `glory_backend_wandorius` se conservó por ser el target reciente del proyecto activo.
- **Archivos:** upstream sentinel `src/core/runtimeInstall.ts` (nuevo), `src/core/diagnose.ts`, `src/cli/index.ts`, `src/test/suite/runtimeInstall.test.ts` (nuevo), `src/test/suite/cliContract.test.ts`; gate `tools/sentinel` (gitlink a `3071171`), `quality-tools.json`, `sentinel.lock.json`, `scripts/quality/{target-maintenance.mjs, target-maintenance-stage.mjs (nuevo), task-check.mjs, reporter.mjs}`, `scripts/quality/tests/target-maintenance.test.mjs` (nuevo), `AGENTS.md` (§9 supervisión automática), `roadmap-sentinel.md`, plan 028A-6.
- **Límites:** con la Fase 1 cerrada, lo pendiente del plan es la Fase 3 (integración del orquestador de wandori.us a `sentinel check --stages` en observe, doble vía) y la Fase 4 (matriz multi-shell); los shims globales en PATH del runtime instalado y la retirada del guard actual se ejecutan cuando la Fase 3 pase cinco tareas reales dentro del presupuesto.

## 028A-6 (SNT-10, Fase 3/observe) — Config v2 completa y doble vía observe del gate agnóstico

- **Qué:** la Fase 3 arranca con los dos pendientes centrales del plan — política v2 en el workspace y la doble vía observe comparando reportes normalizados entre el gate actual y el agnóstico:
  - **`sentinel.config.json` v2 completo (upstream `f29687c`):** el core tolera el envelope v2 (`schemaVersion`/`mode`/`gate`/`guard`/`runtime`/`analyzers`) y extrae las reglas del analizador desde `analyzers.sentinel.config` vía `analyzerSubConfig` (el guard local exige v2 puro; `mode: enforce` conserva la protección actual sin relajarla antes de validar la doble vía). `gateRun.ts`: `GLORY_QUALITY_GATE_TOKEN` por-ejecución con restauración del valor previo (try/finally) — sin filtrarse entre llamadas del mismo proceso.
  - **Doble vía observe:** `observe-compare.mjs` (nuevo) corre `task:check` y `sentinel check <task> --stages` sobre la misma tarea y compara decisiones y hallazgos normalizados (`compare.json` + diff en stdout; exit 0 = coinciden, 1 = difieren, 2 = setup). `stages.mjs` (nuevo) genera el contrato declarativo del core (mismos nombres/tiempos que `stageDefinitions`); `stage-process.mjs` (nuevo) ejecuta una etapa del orquestador como proceso y emite `entries[].findings[]` con el contrato exacto del core.
  - **Paridad de alcance (hallazgo clave):** task:check consulta el guard de ejecuciones pesadas y puede diferir a local-light; el gate agnóstico reutiliza el `scope-manifest.json` del gate actual (`--scope-manifest` + `manifestToScope` en `scope.mjs`, usado por `stages.mjs` y `stage-process.mjs` — este último escribe su propio `changed-files.txt` para `--files-from`). Sin esto el agnóstico corría full mientras el actual corría local-light (204 hallazgos espurios sobre el workspace completo).
  - **Windows:** `runner.mjs` lanza shims `.cmd`/`.bat` con shell + quoting propio (comillas duplicadas estilo cmd, sin EINVAL ni deprecación DEP0190); `adapters/common.mjs` `npmInvocation` con fallback al npm del PATH bajo `node` directo.
- **Pruebas:** orquestador **144/144 PASS** (3 nuevos de observe-integration: contrato de stages, wrapper + exit code, comparador). Upstream **380 PASS** (config v2 + token). Demo real 028A-6: `observe-compare` → **PASS vs PASS, hallazgos idénticos** (local-light, 9 archivos, mismo manifest de alcance). Gate real `task:check -- 028A-6`: sentinel/varsense/custom PASS; frontend FAIL solo por `icon-grid*.test.ts` sin commitear del otro agente (ajeno).
- **Archivos:** gate `sentinel.config.json`, `scripts/quality/{observe-compare.mjs, stages.mjs, stage-process.mjs (nuevos), scope.mjs, runner.mjs, adapters/common.mjs, adapters/sentinel.mjs}`, `scripts/quality/tests/observe-integration.test.mjs` (nuevo); upstream sentinel `src/core/config.ts`, `src/core/gateRun.ts`; `tools/sentinel` (gitlink a `f29687c`), `quality-tools.json`, `sentinel.lock.json`, `roadmap-sentinel.md`, plan 028A-6.
- **Límites:** la activación de `enforce` en el core queda pendiente de validar la doble vía en más tareas reales y resolver diferencias/falsos positivos; también pendientes la migración de `quality-command-guard` al runtime global, la ejecución de VarSense desde Sentinel con paridad demostrada y la Fase 4 (matriz multi-shell).

## 028A-6 (SNT-10, Fase 3/paridad) — Paridad de hallazgos de VarSense entre el gate y su CLI

- **Qué:** el pendiente del plan "Ejecutar VarSense desde Sentinel y demostrar paridad de hallazgos con su CLI/LSP, sin permitir que VarSense cierre la tarea por separado" queda cerrado con evidencia reproducible:
  - **`varsense-parity.mjs` (nuevo):** ejecuta la etapa varsense del gate agnóstico (stage-process → adapter → CLI, la misma invocación que task:check vía `buildVarsenseInvocation`) y el CLI de VarSense **directo** sobre el mismo alcance. La paridad exige el mismo alcance en ambos lados: detectScope escribe `scope-manifest.json` en el reportRoot y `--scope-manifest` se pasa a stage-process (mismo patrón que observe-compare), con el mismo `--files-from`/`--index-dir` del adapter. El `--output` se reemplaza en su posición real (`lastIndexOf`) porque el adapter añade `--files-from`/`--index-dir` después.
  - **Normalización dual:** `normalizeGateFindings` (el reporte de la etapa ya viene normalizado por el adapter) vs `normalizeDirectFindings` (JSON crudo del CLI con `entries[].ruta` + `findings[].range` → base 1-based). `findingKey` = `ruleId:file:line`. Ambas funciones puras exportadas con guard `isDirectRun` para que los tests importen sin ejecutar el CLI.
  - **Resultado:** demo real 028A-6 → **169 hallazgos (gate) = 169 (CLI directo), PARIDAD, exit 0**, mismo alcance full/5 archivos. El reporte `parity.json` registra explícitamente que VarSense no cierra la tarea por separado: la decisión final la toma `sentinel check` con el reporte combinado.
- **Pruebas:** orquestador **147/147 PASS** (3 nuevos de varsense-parity: normalizadores idénticos, diferencias detectadas, integración real con la tarea). Revisión: 3 hallazgos atendidos — alcance compartido vía `--scope-manifest` a stage-process (el wrapper recalculaba su propio alcance con --full), ENOENT no capturado cuando una vía falla (ahora se reporta estado sin leer reportes inexistentes) e import muerto `fileURLToPath`.
- **Archivos:** gate `scripts/quality/varsense-parity.mjs` (nuevo), `scripts/quality/tests/varsense-parity.test.mjs` (nuevo), `roadmap-sentinel.md`, plan 028A-6.
- **Límites:** la activación de `enforce` en el core y la migración de `quality-command-guard` al runtime global siguen pendientes; la Fase 4 (matriz multi-shell) depende del runtime global.

## 028A-8 (tramo 5) — Checkout interno de VarSense como submódulo (`tools/varsense`)

- **Qué:** VarSense dejó de depender de la variable externa `GLORY_VARSENSE_SOURCE_PATH` y pasó a vivir dentro del workspace como submódulo, editable directamente y con el pin versionado en main:
  - `.gitmodules` + gitlink: `tools/varsense` → `https://github.com/1ndoryu/varsense.git` en `e8360927ee92c4067f1f501dd77b951c8bc4f61d` (el commit fijado por el lock). Mismo patrón que `glory-rs`; sin `branch` (pin por commit).
  - `quality-tools.json`: `varsense.sourcePathEnv` → `sourcePath: "tools/varsense"` (relativo al workspace). El lock de varsense queda sin `sourcePathEnv`; el commit lo garantiza el gitlink.
  - `scripts/quality/source-path.mjs`: `resolveConfiguredSourcePath(config, label, { baseDir })` acepta `sourcePath` relativo resuelto contra el root del workspace (portable entre máquinas); absolutas y `sourcePathEnv` GLORY_* siguen funcionando; `validateSourcePath` sigue exigiendo absoluta para `sourcePathRealpath` del lock.
  - `scripts/quality/lockfile.mjs`: `baseDir` en los 4 call sites del resolver; `validateLock` usa un check booleano (no necesita resolver con root); `validateResolvedSourcePaths(workspaceRoot, lock, manifest)` compara realpaths de interno y externo por igual.
  - `scripts/quality/setup.mjs`: `ensureSourcePathReady(name, config)` — en un clon limpio inicializa el submódulo (`git submodule update --init`) y compila el CLI (`npm ci --ignore-scripts` + `buildScript` + opcional `testScript`) si falta `dist`; verifica commit y versión. Un `sourcePath` externo sin CLI sigue fallando con instrucción manual (sin compilar fuera del workspace).
  - Tests: `source-path.test.mjs` con resolución relativa contra `baseDir` y rechazo de relativa sin `baseDir`.
- **Pruebas:** `npm run quality:test` **136/136 PASS**. `quality:setup` verifica ambos tools (varsense por submódulo). Clon limpio: `rm -rf tools/varsense/dist` → `quality:setup` recompila y reporta `2.2.0`. Reutilización del índice con el checkout interno: `cacheHitRate=1.0`, `reused=419`, `reparsed=0`, `peakRssMb=89.5` (antes 137.8). Gate 028A-8 (full automático, `quality-tools.json` está en `fullPatterns`): sentinel/frontend/docs/custom **PASS**; varsense detecta `Variable '--fondo' no esta definida` en `desktop-game-playable.css:240` — hallazgo **real** del bloque GAME-01 ajeno (`91b5f13f`), no falso positivo del índice inverso; rust FAIL por cambios sin commitear del otro agente (clippy/fmt/test en `glory-backend`). No hay hallazgos propios de este tramo.
- **Archivos:** `.gitmodules`, `tools/varsense` (gitlink), `quality-tools.json`, `sentinel.lock.json`, `scripts/quality/{source-path,lockfile,setup}.mjs`, `scripts/quality/tests/source-path.test.mjs`, `AGENTS.md`, `roadmap-sentinel.md`, plan 028A-8.
- **Sentinel/GLORY:** el contrato `sourcePath` relativo es agnóstico del orquestador; VarSense no cambió su core en este tramo (solo la ubicación del checkout consumido).
- **Límites:** Sentinel sigue en `GLORY_SENTINEL_SOURCE_PATH` externo (misma migración pendiente de decidir); el primer run tras cambiar el checkout reanaliza (identity de cache cambia con la ruta del CLI); los fallos ajenos de rust y `--fondo` se corrigen en el bloque GAME-01 del otro agente.

## 028A-8 (tramo 4) — Selección de dependencias con el índice inverso + métricas Fase 0

- **Qué:** se amplió el análisis incremental de VarSense con el índice inverso de variables y métricas por etapa, y se fijó el upstream en el gate:
  - Upstream (ramas `028A-8/persistent-index` y `028A-8/dependency-expansion`, worktree `C:/tmp/varsense-persistent-index`, commits `230e11b` + `e836092` mergeados al `main` consumido y pusheados a GitHub):
    - `classIndexBuilder.ts`: `extraerUsoVariablesDeTexto` captura los usos `var(--x)` por archivo CSS y los persiste en la entrada del índice junto a `classDefinitions`.
    - `persistentIndex.ts`: campo `variableUsages` en la entrada, `buildVariableReverseIndex` (variable → consumidores) y bump de `PARSER_VERSION` a '2' (invalida snapshots previos).
    - `tokenRules.ts`: `analyzeTokenRules` acepta un `variableUsageIndex` opcional; token-unused consulta el mapa (O(vars+usos)) en lugar de escanear el texto de todos los documentos por variable (O(vars×texto)). El fallback sin índice extrae los nombres usados una sola vez con la misma semántica que la captura (`var(\s*--x)`, tolera espacios) y conserva el comportamiento previo para LSP/editor.
    - `cli/index.ts`: con `--files-from` + `--index-dir` activos, se construye el índice inverso desde el snapshot y el análisis documental abre **solo los archivos scoped**; los hallazgos documentales se restringen al alcance (guard `isScopedFile` restaurado tras revisión). Métricas Fase 0 en el JSON: `filesDiscovered/filesAnalyzed/filesReused/cacheHitRate/peakRssMb`.
    - `scripts/smoke-tramo4.mjs` (nuevo) + `package.json`: smoke end-to-end — full scan sin falso unused del usado, reutilización total (`reparsed=0`, cacheHitRate=1), `--files-from` scoped con exactitud vía índice inverso (sin falso positivo), invalidación por cambio de uso (quitar `var(--x)` repara el token-unused).
  - Gate local: `quality-tools.json` fija `varsense.commit=e8360927ee92c4067f1f501dd77b951c8bc4f61d` (capabilities `filesFrom` + `persistentIndex` siguen en true); `sentinel.lock.json` regenerado con `--write` y `--check` PASS.
- **Pruebas:** upstream — tsc, lint, check:core, smoke LSP, `smoke:persistent-index` y `smoke:tramo4` PASS. Gate — `npm run quality:test` 134/134 PASS; gate real `task:check -- 028A-8` PASS con `filesAnalyzed=16/319` (solo scoped abiertos), `cacheHitRate=1.0`, `reused=364`, `reparsed=0`. El tiempo residual de varsense (~11 s) es descubrimiento de archivos + hashing de contenido, no análisis documental.
- **Archivos:** upstream `src/core/{classIndexBuilder,persistentIndex,tokenRules}.ts`, `src/cli/index.ts`, `scripts/smoke-tramo4.mjs`, `package.json`; gate `quality-tools.json`, `sentinel.lock.json`, `roadmap-sentinel.md`, `roadmap.md`, plan 028A-8.
- **Revisión:** 3 hallazgos atendidos — regresión de guard scoped en hallazgos documentales con `--files-from` sin `--index-dir` (restaurado), fallback no toleraba `var( --x )` con espacios ni era consistente con la captura del índice (extracción única con regex compartida), y métrica `scopeExpandedFiles` engañosa (eliminada).
- **Límites:** watchers/LSP persistentes, índices globales de Sentinel, métricas RSS/p50-p95 del orquestador y benchmark siguen pendientes.

## 028A-8 (tramo 3) — Fijación del upstream y activación del índice persistente en el gate

- **Qué:** se activó el índice persistente de VarSense en el gate real:
  - Merge fast-forward `858ec62`→`11f0932` de la rama `028A-8/persistent-index` al `main` consumido de VarSense (`GLORY_VARSENSE_SOURCE_PATH`); el worktree de desarrollo queda intacto.
  - Recompilación del `dist/` del checkout consumido (`esbuild --minify`); `dist/` está en `.gitignore`, por lo que el lock (basado en `git archive HEAD`) no cambia.
  - `quality-tools.json`: `varsense.commit=11f0932de5e66d88a79fdaa960ca77e5774d0a33` y `capabilities={ filesFrom: true, persistentIndex: true }`.
  - `sentinel.lock.json` regenerado con `npm run quality:lock -- --write` (backup `.bak` conservado) y `--check` PASS.
- **Pruebas:** gate real `task:check -- 028A-8` PASS con `varsenseScope.persistentIndex={ enabled: true, indexDir: <branchCache>/varsense }`; snapshot persistente creado en `.quality-reports/branches/<key>/cache/varsense/varsense-index.json` con 363 entradas; segunda ejecución: `loaded=363, reused=364, reparsed=0` (ningún archivo se re-parseó). El tiempo de varsense sigue ~11 s dominado por el análisis documental de 682 candidatos (los índices globales se conservan para exactitud).
- **Archivos:** `quality-tools.json`, `sentinel.lock.json`, checkout upstream varsense (merge), `roadmap-sentinel.md`, `roadmap.md`, plan 028A-8, `Agente/completados/tareas-2026-08-04.md`.
- **Sentinel/GLORY:** contrato de capacidad versionado en lock; el flag se activa solo con la capacidad declarada.
- **Límites:** selección de dependencias con el índice inverso (ampliar `--files-from` con consumidores), watchers/LSP, índices globales de Sentinel y métricas RSS/p50-p95 siguen pendientes.

## 028A-8 (tramo 2) — Índice persistente de VarSense entre ejecuciones

- **Qué:** persistencia por archivo de los resultados del análisis en el CLI de VarSense (upstream), en rama `028A-8/persistent-index` del repo (`C:/Users/Owner/.../.agent/varsense`), worktree `C:/tmp/varsense-persistent-index`:
  - `src/core/persistentIndex.ts` (nuevo): `PersistentIndexStore` (getEntry/setEntry/removeEntry + stats), `FilePersistentIndexStore` con carga validada por `schemaVersion`+identidad, guardado atómico (`tmp`+rename) y **reconciliación con disco al cargar**: en un proceso nuevo las caches en memoria están vacías, así que cada entrada se valida con `fs.stat` y las de archivos eliminados entre ejecuciones se expulsan (el snapshot no crece sin límite y el índice inverso no referencia consumidores inexistentes). Identidad = `toolVersion + configHash + parserVersion` (`indexIdentity`/`configHashFor` con claves ordenadas). Índice inverso `buildReverseIndex` (token/class → consumidores) y `pathKeyForIndex` normalizado por plataforma.
  - `src/core/classIndexBuilder.ts` y `src/core/variableIndexBuilder.ts`: **store-first** — se calcula el SHA-256 del contenido en disco antes de abrir/parsear; si `stored.hash === hash` se reutiliza la entrada (stats.reused) y el archivo nunca se vuelve a parsear; si cambia, se re-parsea y se actualiza (stats.reparsed). `invalidateFile` y la poda de archivos ausentes también expulsan la entrada persistente (stats.removed).
  - `src/cli/index.ts`: flag `--index-dir <path>` en `scan`/`orphan-classes`/`all`; `createIndexStore` crea `FilePersistentIndexStore` con identidad completa, carga y guarda al finalizar; el JSON del reporte expone `cache: { enabled, identity, loaded, reused, reparsed, removed, entries }`. Sin `--index-dir` el comportamiento es idéntico al previo (retrocompatible con `858ec62`).
  - `package.json` + `scripts/smoke-persistent-index.mjs` (nuevo): smoke end-to-end — run1 construye, run2 reutiliza todo (`reparsed=0`, findings idénticos), run3 re-parsea solo el archivo modificado, run4 expulsa la entrada del archivo borrado (poda por disco) y run5 invalida todo por cambio de config (identidad distinta, `loaded=0`). Fixture `fixtures/persistent-index/`.
  - Gate local: `scripts/quality/adapters/varsense-contract.mjs` añade `--index-dir <cacheRoot>/varsense` y `scope.persistentIndex` **solo cuando la capacidad está declarada**; `policy.mjs` y `lockfile.mjs` validan la clave de capacidad `persistentIndex`; test nuevo en `varsense-contract.test.mjs`. La capacidad NO está activa en `quality-tools.json` (el checkout fijado `858ec62` no soporta el flag), por lo que el gate sigue verde con el upstream actual.  - **Pruebas:** upstream — `tsc`, `compile:tests` (esbuild), `eslint` (0 problemas), `check:core` (sin vscode en core), `smoke:lsp` y `smoke:persistent-index` PASS. Gate — `npm run quality:test` **133/133 PASS**. Gate real `task:check -- 028A-8 --profile docs` PASS (sin `--index-dir` porque la capacidad no está fijada, como se diseñó). Esta es evidencia histórica de 028A-8; el protocolo de worktrees se fijó después en `e53a8dc5b73c02894b852391e160bf1a4adf13b3`.
- **Archivos:** upstream `src/core/{persistentIndex,classIndexBuilder,variableIndexBuilder}.ts`, `src/cli/index.ts`, `scripts/smoke-persistent-index.mjs`, `fixtures/persistent-index/`, `package.json`; gate `scripts/quality/adapters/varsense-contract.mjs`, `policy.mjs`, `lockfile.mjs`, `tests/varsense-contract.test.mjs`. Commit upstream: `11f0932` en `028A-8/persistent-index` (el `main` consumido queda en `858ec62` intacto).
- **Sentinel/GLORY:** contrato CLI agnóstico del analizador (editor-agnostic, sin vscode); la identidad de cache y la política de invalidación viven en VarSense; el gate solo pasa el directorio por rama. Revisión: 3 hallazgos atendidos — poda de entradas obsoletas entre ejecuciones (stat al cargar), documentación del bump obligatorio de `PARSER_VERSION` al cambiar semántica de parseo/extracción, y nota de que el store es solo CLI (hash de disco; no conectar a LSP con documentos en memoria).
- **Límites:** falta sync/fijación del upstream para activar `capabilities.persistentIndex` en `quality-tools.json` y regenerar el lock; selección de dependencias con el índice inverso (ampliar `--files-from` con consumidores); watchers/LSP; índices globales de Sentinel; métricas RSS/p50-p95.

## 028A-8 (tramo 1) — Alcance efectivo honesto y manifiesto compartido

- **Qué:** se corrigió la degradación full→local-light del quality gate y se generó un único manifiesto de alcance compartido entre etapas:
  - `scripts/quality/scope.mjs`: `resolveFullDecision` puro separa `requestedFull` (--full/--ci), `automaticFull` (fullPatterns/migraciones/config), `effectiveFull` (lo que realmente se ejecuta) y `heavyDeferred` (guard). Un full diferido ya no vuelve a ser full por `automaticFull`. Se registran `deletedFiles` y se escribe `scope-manifest.json` (schemaVersion 1, cambiados, eliminados, hashes SHA-256 de contenido, perfiles, dependencias locales, decisión full y motivo) de forma atómica; `changed-files.txt` se conserva como transporte plano compatible con el contrato `--files-from` de Sentinel/VarSense.
  - `scripts/quality/task-check.mjs`: el lease pesado se solicita cuando `scope.effectiveFull && runsAllStages && !args.heavyDeferred`, incluyendo automaticFull (antes solo con --full/--ci); si el guard bloquea, se re-detecta el alcance con `heavyDeferred` y `effectiveFull=false` real.
  - `scripts/quality/cache.mjs`: fingerprint v5 — el modo usa `scope.effectiveFull` (un automaticFull permitido queda como full aunque `context.full` sea false; un full diferido queda local-light) y marca borrados/renombres.
  - `scripts/quality/reporter.mjs`: `formatScope` muestra el motivo (`full · ejecución incremental (heavy-deferred)`); el reporte JSON expone `requestedFull`, `automaticFull`, `effectiveFull`, `fullReason`, `heavyDeferred` y `deletedFiles` sin romper el schema público.
  - `scripts/quality/adapters/custom.mjs` y `adapters/rust.mjs`: consumen el alcance efectivo (custom escopeado en full diferido; rust con clippy/test solo cuando `effectiveFull`).
  - `scripts/quality/run-frontend-tests.mjs`: acepta `--scope-manifest <path>` para reutilizar el manifiesto del gate y no repetir descubrimientos Git/glob.
- **Pruebas:** `npm run quality:test` **131/131 PASS** (nuevos tests: matriz de `resolveFullDecision`, cache por alcance efectivo, reporter con full diferido y motivo, rust con alcance efectivo). `node --check` de los módulos modificados PASS.
- **Gate real:** `npm run task:check -- 028A-8` con los mains externos fijados → el automaticFull (edición de `scripts/quality/`) intentó el lease, el guard lo difirió y el reporte quedó `Scope: full · ejecución incremental (heavy-deferred) · 24 archivos`; sentinel/varsense/custom/docs PASS y VarSense aplicó `--files-from` (`requestedScopedAnalysis: true, applied: true`). El stage frontend quedó en ERROR por `frontend/src/api/generated` ausente en este checkout (TS2307 en 4 services; preexistente, sin relación con el tramo).
- **Archivos:** `scripts/quality/{scope,task-check,cache,reporter,stage-definitions,run-frontend-tests}.mjs`, `scripts/quality/adapters/{custom,rust}.mjs`, tests `{scope,cache,reporter,rust-adapter}.test.mjs`, `roadmap.md`, `roadmap-sentinel.md`, plan 028A-8.
- **Sentinel/GLORY:** contrato local del orquestador agnóstico; sin reglas nuevas ni cambios en el core de Sentinel/VarSense.
- **Límites:** índices persistentes de VarSense entre ejecuciones, watchers/LSP, índices globales de Sentinel, métricas por etapa (RSS/CPU/cache hit) y presupuestos p50/p95 quedan para los siguientes tramos de 028A-8. El `scope-manifest.json` se escribe dentro de `.quality-reports/branches/<branch-key>/<task-id>/`, por lo que queda cubierto por la retención por rama.

## 297A-67 — Pincel de altura del Editor de mapa (vértices compartidos)

- **Qué:** el editor 2D del Bosque añade la tool `height`: `TERRAIN_HEIGHT_VALUES` discretos allowlisted (0/0.25/0.5/1/1.5/2/2.5/3/4, no negativos, dentro del rango del contrato) con `isAllowedHeight` fail-closed y `activeHeight` en el estado. `terrainVertexAt` resuelve mundo → vértice de la malla (chunkSize+1)² devolviendo refs a TODOS los chunks que comparten el vértice (interior, borde X/Z o esquina de 4), fail-closed fuera de bounds. `paintHeight` actualiza el vértice en todos los chunks compartidos (bordes sincronizados, sin descuadres) y commitea solo si algo cambia (arrastre limpio). El canvas sombrea las celdas por el promedio de sus 4 vértices y marca los puntos de vértice solo con la tool altura; el toolbar expone el botón "altura" y un select de nivel.
- **Archivos:** `frontend/src/features/desktop/apps/game-playable/game-map-editor-height.ts` (nuevo, core puro), `game-map-editor-height.test.ts` (12 tests), `game-map-editor-core.ts` (tool, valores, `activeHeight`, export `commit`), `game-map-editor-canvas.ts` (sombreado/puntos), `game-map-editor-toolbar.ts` (botón+select), `game-map-editor.ts` (wiring pointer/select), `desktop-game-playable.css` (clase compartida del select), `game-map-editor.test.ts` (asserts de vista).
- **Gate:** `task:check -- 297A-67` PASS en modo full/heavy-deferred (sentinel, varsense, type-check frontend, custom, docs). 12/12 tests nuevos + 4/4 de vista PASS; 84/88 de la selección (4 fallos de `game-map-editor-core.test.ts` son PREEXISTENTES del WIP ajeno 038A-3: el fixture `game-fixture-map.ts` cambió `terrainSurfaces` tras el cierre de 297A-66 y los asserts de superficie asumen la celda (0,0) en suelo; se verificó que fallan también contra HEAD sin mis cambios).
- **Entorno reparado:** `frontend/node_modules` estaba incompleto (faltaba vitest) → `npm install` restauró devDependencies (no cambió package-lock). Los clientes Orval de `frontend/src/api/generated/` estaban ausentes (fallo preexistente TS2307 en 4 services, ya documentado por 028A-8) → `npm run codegen:local` los regeneró para desbloquear el type-check.
- **Sentinel/GLORY:** sin reglas nuevas; `commit` se exporta por segundo consumidor real; `isAllowedHeight`/`TERRAIN_HEIGHT_VALUES` viven en el core junto a los valores de superficie (decisión del bloque).
- **Límites:** el pincel pinta solo vértices de chunks existentes (no crea terreno ni redimensiona bounds); la altura aún no se representa en 3D (Assets 3D); el sombreado del canvas es local del editor; sin caminos ni tipos de superficie adicionales.
