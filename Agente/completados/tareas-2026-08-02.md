# Tareas completadas — 2026-08-02

## 028A-1 — Orden único de planes y triage de alertas quality

- **Qué:** se añadió una cola única priorizada al roadmap, un recordatorio de reconciliación/archivo de planes y una política para no ignorar warnings aunque el gate diga `PASS`.
- **Archivos:** `roadmap.md`, `Agente/planes/plan-triage-alertas-quality-2026-08-02.md`, `Agente/documentacion/indice-documentacion-2026-07-29.md`.
- **Gotchas:** `297A-48` es un full baseline (250 Sentinel, 77 VarSense, 40 custom), no una lista de regresiones nuevas; su delta real fue 19 hallazgos. Se separaron warnings productivos, recurrentes y falsos positivos de helpers `#[cfg(test)]`, JSDoc, clases dinámicas y aliases semánticos. El gate dejó Sentinel/VarSense/frontend/docs en PASS, pero Rust falló por `sccache`/conexión interrumpida al compilar dependencias (`os error 10054`), ajeno a estos documentos.
- **Sentinel:** el plan exige delta `new/recurrent/resolved`, fixtures para falsos positivos y owner/fecha para cada excepción; no se modificaron reglas en este bloque.
- **GLORY:** no aplica; la mejora es documentación y gobernanza específica del proyecto.

## 028A-2 — Quality gate local ligero y suite completa explícita

- **Qué:** se separó el feedback local de la cobertura completa: Rust local ejecuta `fmt/check`, mientras `clippy/test` quedan para `--full`/`--ci`; el frontend usa `test:changed`; la selección full dejó de dispararse por cualquier cambio de manifiesto frontend; caché, reporte y recordatorios distinguen `local-light`, `full` y `ci`.
- **Archivos:** `quality.config.json`, `scripts/quality/adapters/rust.mjs`, `scripts/quality/preflight.mjs`, `scripts/quality/cache.mjs`, `scripts/quality/reminders.mjs`, `scripts/quality/reporter.mjs`, `scripts/quality/tests/cache.test.mjs`, `scripts/quality/tests/reminders.test.mjs`, `scripts/quality/tests/rust-adapter.test.mjs`, `frontend/package.json`, `roadmap.md`, `roadmap-sentinel.md`, `Agente/planes/plan-triage-alertas-quality-2026-08-02.md`.
- **Gotchas:** `297A-49` tardó 533833 ms; Rust consumió 483037 ms (90,5 %) y expiró durante `cargo test` con un target frío. El modo ligero no equivale a cobertura completa: antes de cerrar una fase o publicar se debe ejecutar `npm run task:check -- <ID> --full` o `--ci`.
- **Sentinel:** se añadieron pruebas del contrato del adaptador y del recordatorio; no se silencian warnings ni se cambia su severidad.
- **GLORY:** no aplica; el contrato de selección es específico del quality gate de este proyecto y queda documentado para extraerlo al orquestador agnóstico después.

## 028A-3 — Dev launcher: auto-recuperación de drift de migraciones (42P07)

- **Que:** `npm run dev` fallaba con `error: while executing migration 20260801140000: ... la relación «game_map_versions» ya existe`. La BD local `glory_backend_wandorius` tenía objetos de GAME-01 creados fuera de sqlx (prototipado/agente paralelo) sin fila en `_sqlx_migrations` (historial llegaba solo a `20260801130000`). El detector de `glory-rs/scripts/dev.mjs` solo reconocía `VersionMissing|VersionMismatch|previously applied but has been modified`, así que salía con `No se pudieron aplicar las migraciones locales.` en vez de auto-recuperarse.
- **Arreglo raíz:** se extendió el detector para que el drift de `already exists` / `ya existe` (SQLSTATE 42P07, objetos presentes fuera del historial de sqlx) entre al flujo de reset del schema `public` de desarrollo y reaplicación, igual que las otras clases de incompatibilidad. Una tabla creada fuera de sqlx + sin registro es el mismo drift que el historial faltante; el reset del dev-DB es la cura diseñada por el propio launcher.
- **Verificación real:** `npm run dev` → `Migraciones incompatibles o drift en glory_backend_wandorius; reseteando schema public de desarrollo.` → reset → reaplicación completa de la cascada (incluye 140000–170000 del juego) → `Iniciando backend (cargo run --bin glory-backend) y frontend (vite)` → Vite en `http://localhost:5174/`. El proxy dev devuelve `ECONNREFUSED` solo mientras el backend compila (normal).
- **Archivos:** `glory-rs/scripts/dev.mjs` (submódulo `glory-rs`, commit `f3ab77f`).
- **Gotchas:**
  - El fix vive en el submódulo `glory-rs` (repo `github.com/1ndoryu/glory-rs-framework.git`). El puntero del submódulo NO se actualizó en el repo padre porque `main` del submódulo tiene un commit ajeno sin pushear (`e6ec191 fix: harden cargo tmp cleanup`); el push de `main` del submódulo queda diferido para coordinar con el otro agente.
  - No se tocó ningún archivo del otro agente (GAME-01 / 297A-48 / 297A-50) ni el `roadmap.md` (tiene ediciones concurrentes).
  - El perfil de PowerShell de la terminal imprime un warning de `SUPERMEMORY_API_KEY` al abrir shells; es ruido ajeno, no bloquea.
- **Sentinel:** no aplica (archivo JS del framework, fuera de los analyzers del repo).
- **GLORY:** el fix ya vive en el dev launcher agnóstico (`glory-rs/scripts/dev.mjs`), que pertenece al framework.

## 028A-4 — Botones de superficies OS sin `boton-grande` (tamaño gobernado por el chrome)

- **Que:** el botón "entrar" (`account-app__submit`) se renderizaba más grande que los botones secundarios de la misma ventana porque llevaba la clase `boton-grande` (font-size 15px) frente a la receta compacta `.boton` (13px). Según el manual de identidad (§Botones según superficie, nota [018A-1 F1]) y el comentario del editor: dentro de superficies OS el tamaño lo gobierna el chrome, no el contenido. Se corrigió y se revisó todo el código: solo había otro botón con el mismo problema.
- **Qué:** se eliminó `boton-grande` de (1) el botón de envío de la ventana Cuenta (`account-view.ts`, modos entrar/registrar/recuperar) y (2) "proceder al pago" del modal de checkout (`article.ts`); se borró también la receta CSS `.boton-grande` de `components.css` (quedaba huérfana: definir sin usar dispara `claseHuerfana` en VarSense).
- **Archivos:** `frontend/src/features/runtime/account-view.ts`, `frontend/src/pages/article.ts`, `frontend/src/styles/components.css`.
- **Verificación real:** `npx tsc --noEmit` completo con `exit=0`; el usuario confirmó visualmente en el navegador que "ya se ve bien". (Durante la edición, el tool de reemplazo se comió las líneas de cierre del objeto en `account-view.ts` y rompió el parseo — se restauró a mano y `tsc` quedó limpio.)
- **Gotchas:** los diagnósticos de Sentinel en `components.css` (línea >600, botón fuera de Button.css, `.notificaciones__item`, `.comercio__producto h3`) son pre-existentes y ajenos a este bloque; no se tocaron para no mezclar trabajo con el otro agente.
- **Sentinel:** no aplica regla nueva; la regla de recetas já está documentada en el manual. El whitelist de `.quality-tools/sentinel` sigue listando `boton-grande` como receta conocida (inofensivo; la clase ya no existe).
- **GLORY:** no aplica (CSS/específico de wandori.us por ahora; no hay componente de botón grande agnóstico en uso).

## 028A-5 — Novedades: popover de campana + admin "novedades" con borrado

- **Qué:** La app `notifications` dejó de ser una ventana: la campana (barra de menú desktop y launcher móvil) abre un popover compacto de ~300px anclado al botón, con lista de hasta 40 avisos, scroll de ~260px (`max-height`), cabecera con recargar, pie con "marcar todo como leído", marcador "■" en títulos no leídos, contador en la campana y cierre por clic fuera/Escape con `aria-expanded`. El admin de novedades vive en la app Admin como tab "novedades": listado de avisos, modal "+ nuevo aviso", cambio de estado con `createSelect` y botón "eliminar" con confirmación (`showConfirm`) que borra también los avisos publicados; las lecturas se limpian en cascada (`ON DELETE CASCADE`). El tag de estado muestra etiqueta en español (`borrador`/`publicado`/`archivado`) en una línea (`white-space: nowrap`). Se retiró el código legacy (`notifications-view.ts`, `notifications-admin.ts`) y los estilos `.notificaciones`/`.notificacionesAdmin`.
- **Backend:** borrado con ruta `DELETE /api/admin/notifications/{id}` — repo (`NotificationRepository::delete` con `rows_affected > 0`), service (devuelve `NotFound` si no existe), handler `delete_notification` con utoipa → 204/404, `AdminUser` + session cookie. Cliente regenerado con codegen local.
- **Archivos:** `src/handlers/notifications.rs`, `src/repositories/notification_repo.rs`, `src/services/notification_svc.rs`, `frontend/src/features/notifications/notifications-popover.ts` (nuevo), `frontend/src/features/notifications/notifications-store.ts`, `frontend/src/features/notifications/notifications-admin.ts` (borrado), `frontend/src/features/notifications/notifications-view.ts` (borrado), `frontend/src/pages/admin-notifications.ts` (nuevo), `frontend/src/pages/admin.ts`, `frontend/src/services/notifications.service.ts`, `frontend/src/features/desktop/components/desktop-menu-bar.ts`, `frontend/src/features/mobile/mobile-launcher.ts`, `frontend/src/features/runtime/app-registration.ts`, `frontend/src/main.ts`, `frontend/src/styles/components.css`, `roadmap.md`.
- **Gotchas:** `npm run codegen` (por defecto) consulta el backend vivo que corría el binario viejo sin la ruta DELETE; se resolvió con `npm run codegen:local` (cargo `--emit-openapi` + orval). `mod.rs` ya no está en el diff porque el agente de `297A-52` incluyó `notifications::delete_notification` en su commit. El marcador "■" del título es intencional: es el `::before` del boceto aprobado para títulos no leídos. Los `WeakMap`s de la lista (`notificacionesListCleanups`/`notificacionesListGenerations`) están presentes; si faltan, la lista no limpia sus listeners.
- **Verificación real (navegador):** admin tab "novedades": borrar aviso publicado (confirm → toast "aviso eliminado" → estado vacío), crear aviso (modal), cambiar estado borrador→publicado (tag en español "publicado"); popover: abre desde campana (desktop y launcher móvil <768px), recargar, "marcar todo como leído" quita contador y marcador "■", cierre por clic fuera y Escape. Test aviso limpiado al final (DB sin residuos).
- **Sentinel:** `npm run task:check -- 028A-5` PASS (37 archivos; sentinel 0 errores / varsense 0 errores / rust fmt+check / type-check / docs / custom). `npm run self-check -- -TareaId 028A-5` PASS. `cargo clippy -D warnings` PASS, `npx tsc --noEmit` PASS, `npx vitest run` PASS (76 archivos / 523 tests).
- **GLORY:** no aplica; popover, tab admin y borrado dependen del modelo de notificaciones y del diseño del OS.

## 028A-7 — El label de cuenta muestra solo el nombre de usuario

- **Qué:** Se eliminó el prefijo redundante "Cuenta ·"/"cuenta ·" del botón de cuenta en la barra de menú desktop y en el launcher móvil: cuando hay sesión, el label muestra solo el nombre derivado de la parte local del email (antes de `@`), expuesto por `/auth/me`. Se añadió `userEmail?: string | null` (opcional para no romper los ~60 `authStore.set` existentes) a `AuthState`, se puebla/limpia en `AuthService.me()` y `logout()`, y el helper `authAccountName(state)` devuelve la parte local del email con fallback por capacidad (`admin`/`cuenta`). Sin sesión sigue mostrando "Entrar"/"entrar".
- **Archivos:** `frontend/src/store.ts`, `frontend/src/services/auth.service.ts`, `frontend/src/features/desktop/components/desktop-menu-bar.ts`, `frontend/src/features/mobile/mobile-account-control.ts`, `frontend/src/features/desktop/components/desktop-menu-bar.test.ts`, `roadmap.md`.
- **Gotchas:** el helper se comentó inicialmente con el ID provisional `028A-6` (la tarea se numeró `028A-7` después de las ediciones); se corrigió el ID en los comentarios para consistencia. `userEmail` es opcional porque `createStore.set` reemplaza el valor completo y hay muchos `authStore.set` sin ese campo; hacerlo obligatorio habría roto el type-check. `AccountView` (ventana Cuenta) no se tocó: sigue mostrando "cuenta" y "sesión activa · admin", que es el contenido de la ventana, no el label del botón.
- **Verificación real (navegador):** logueado como admin, la barra desktop muestra solo "wandorius" (antes "Cuenta · wandorius") y el launcher móvil <768px muestra solo "wandorius". El dev server se había quedado con un `glory-backend.exe` viejo (bloqueaba el binario con "Acceso denegado") y varios Vite huérfanos en 5173/5174/5175; se limpiaron los procesos stale del proyecto y se relanzó `npm run dev` limpio (backend 3000 OK, frontend 5174 OK). Un Vite de otro proyecto (`test1/freellmapi`) en 5173 se respetó.
- **Sentinel:** `npm run task:check -- 028A-7` PASS (14 archivos; sentinel 0 errores / varsense 0 errores / frontend type-check / docs / custom). `node scripts/quality/run-frontend-tests.mjs` PASS: 79 archivos / 541 tests.
- **GLORY:** no aplica; el helper `authAccountName` y el label dependen del modelo de sesión y del chrome del OS. Si en el futuro otro proyecto reutiliza el patrón "nombre visible desde email de sesión", podría extraerse al framework.

## 028A-10 — Release v3 con árbol canónico del escritorio

- **Qué:** se publicó la release v3 del workspace (`workspace_releases`), activa por `MAX(version)`, con el árbol canónico del escritorio: `documentos` + 4 subcarpetas (imágenes/audio/vídeo/documentos), `projects`, `profile`, `about`, `settings`, `admin`, `trash` (Papelera), `store`, `orders` y `downloads` (14 nodos). Recupera los nodos de comercio y Papelera que la v2 (018A-87) había perdido y nunca tuvo (la v2 se escribió a mano y se saltó trash/store/orders/downloads), y excluye `snake` (nodo fantasma sin app registrada) y `game/game3d/gamePlayable` (prototipos GAME-01 ocultados deliberadamente). Migración `20260802010000_028a10_release_v3.up.sql` + `.down.sql` (rollback: `DELETE FROM workspace_releases WHERE version = 3`).
- **Archivos:** `migrations/20260802010000_028a10_release_v3.up.sql`, `migrations/20260802010000_028a10_release_v3.down.sql`, `Agente/planes/plan-gobernanza-workspace-2026-08-02.md`, `roadmap.md`, `Agente/completados/tareas-2026-08-02.md`.
- **Gotchas:** el ID original 028A-8 colisionaba con la tarea de optimización Sentinel/VarSense ya existente en el roadmap → se renumeró todo el plan a 028A-10..14 y se renombró/re-aplicó la migración (`DELETE FROM workspace_releases WHERE version = 3` + `DELETE FROM _sqlx_migrations WHERE version = 20260802010000` → `cargo sqlx migrate run` re-aplica). Bosque/Bosque 3D/Bosque·prueba visibles en dev son el fallback `withLocalPrototypeNodes` de `frontend/src/features/runtime/workspace/local-development-release.ts` (solo `import.meta.env.DEV`, documentado [018A-92]); producción nunca los reintroduce. `.env` stale apunta a kamples → usar siempre `DATABASE_URL='postgres://postgres:root@localhost:5432/glory_backend_wandorius'` explícito para sqlx. Verificado en navegador: desktop muestra Papelera/Tienda/Pedidos/Descargas, el launcher móvil 390px los muestra en `mobileOrder` correcto y la app Papelera renderiza "La papelera está vacía." sin errores.
- **Verificación real:** `GET /api/workspace/release` devuelve `version: 3` con 14 nodos (about, admin, documentos, documentos-audio, documentos-documentos, documentos-imagenes, documentos-video, downloads, orders, profile, projects, settings, store, trash). Gate `npm run task:check -- 028A-10` PASS (sentinel/varsense/rust/frontend/custom) tras añadir el bloque del plan al roadmap (docs exigía que la tarea apareciera).
- **Sentinel:** no aplica regla nueva (migración SQL + MDs). Los warnings pre-existentes de `confirm.ts`/`modal.ts` (`dom-access-outside-platform`) quedan registrados para su bloque.
- **GLORY:** no aplica; el árbol canónico es contenido específico de wandori.us. La práctica "la release pública no debe referenciar nodos rotos" alimenta la fase 028A-11 (guard en `publish`).

## 028A-11 — Guard de coherencia en `publish` (gobernanza del workspace)

- **Qué:** guard definitivo de coherencia al publicar releases (`WorkspaceService::publish`): (1) validación estructural del árbol con `validate_release_tree` (tipos `folder|app|shortcut|resource`, parentId válido, detección de ciclos por walk de parentId, `requires` allowlist, límites de 500 nodos y longitudes, `publicLocator` solo en resource/shortcut); (2) validación de `publicLocators` reutilizando `validate_public_locators_in_tree`; (3) refs de recursos contra BD vía `resource_repo::find_broken_public_refs` — deben ser `active + ready + public`, y si no, 422 `validation_error` con `details.brokenRefs` (nuevo `AppError::ValidationDetails` + `ErrorResponse.details`); (4) summary/diff persistidos en la release (`summary` JSONB `{added, removed, modified, nodeCount}` + `diff_from` del release anterior), calculados con `compute_release_summary` por igualdad JSON. Migración `20260802020000_028a11_release_summary` (up/down) añade las columnas. `WorkspaceRepository::list_releases` nuevo. Contrato OpenAPI/Orval regenerado (`WorkspaceRelease.summary/diffFrom`, `ErrorResponse.details`).
- **Archivos:** `src/models/workspace/release_validation.rs` (nuevo), `src/models/workspace/mod.rs`, `src/errors/mod.rs`, `src/repositories/workspace_repo.rs`, `src/repositories/resource_repo.rs`, `src/services/workspace_svc.rs`, `tests/workspace_publish.rs` (nuevo), `migrations/20260802020000_028a11_release_summary.up.sql` + `.down.sql`, `frontend/src/api/**` (generado, ya en HEAD), `roadmap.md`, `Agente/completados/tareas-2026-08-02.md`.
- **Gotchas:** `validate_release_tree` acepta `app` y `requires: admin` (el overlay los prohíbe; el release público sí los permite). El filtro de ciclos requería `*parent != "desktop"` (comparar `&str` con `str` no compila). La migración 028a10 dio checksum mismatch al editar el comentario tras aplicarla → `cargo sqlx migrate revert` + re-run. **Tests de integración pendientes:** el cooldown de 180 min del guard bloqueó `cargo test --test workspace_publish`; el usuario rechazó `--allow-heavy`/`GLORY_QUALITY_ALLOW_HEAVY` (tarea 028A-15). Colisión de IDs: el otro agente usó `028A-10/11/12` para Sentinel; el plan de gobernanza conserva sus IDs (ya commiteados/pusheados).
- **Verificación real:** `cargo check` PASS, `cargo check --tests` PASS, gate local-light `npm run task:check -- 028A-11` PASS (sentinel/varsense/rust fmt+check/frontend/docs/custom). Migración aplicada y columnas `summary`/`diff_from` verificadas en BD. Tests de integración NO ejecutados (cooldown).
- **Sentinel:** no aplica regla nueva; los warnings pre-existentes de `confirm.ts`/`modal.ts` quedan registrados.
- **GLORY:** no aplica; la validación del árbol de release es específica de wandori.us. El patrón "guard de publicación con 422 detallado" podría generalizarse al framework si otro proyecto lo necesita.

## 028A-9 — Cobertura Bash del guard global de calidad

- **Qué:** se corrigió el bypass observado en Git Bash: el interceptor solo estaba instalado como función/perfil PowerShell, por lo que una shell Bash podía ejecutar `rustfmt`, `cargo check`, `cargo test` y `cargo clippy` directamente. Se añadió `global-quality-guard.sh`, cargado desde `.bashrc`, `.bash_profile` y `BASH_ENV`, con resolución del `quality.config.json` del workspace actual y las mismas recomendaciones de `npm run task:check -- <TareaId>`. `rustfmt` directo ahora también es una validación bloqueada.
- **Archivos:** `scripts/quality/global-quality-guard.sh`, `scripts/quality/install-global-guard.ps1`, `scripts/quality/quality-command-guard.mjs`, `scripts/quality/tests/quality-command-guard.test.mjs`, `roadmap.md`.
- **Gotchas:** los perfiles Bash se instalaron localmente; las shells ya abiertas deben reiniciarse para heredar el cambio. Una invocación absoluta (`C:\\Users\\...\\cargo.exe`) o una shell lanzada con `--noprofile --norc` no puede interceptarse desde un script del repositorio y requiere enforcement en la capa que inicia el agente. No se ejecutaron tests Rust ni compilaciones pesadas.
- **Sentinel:** se añadió cobertura unitaria para `rustfmt` directo; el guard sigue siendo agnóstico y resuelve la política por workspace/rama.
- **GLORY:** no aplica; es tooling agnóstico del proyecto.

## 028A-10 — Sentinel como plano único de calidad

- **Qué:** se corrigió el plan global para no crear un tercer sistema `GloryQuality`. Sentinel será el runtime/CLI único de política, intercepción, cooldown, scope, caché, orquestación y reportes. VarSense queda como analizador especializado invocado por Sentinel mediante contrato de plugin; puede conservar CLI/LSP para el editor, pero no tendrá gate, scheduler ni reporte de cierre propios.
- **Archivos:** `Agente/planes/plan-global-quality-guard-agnostico-2026-08-02.md`, `roadmap.md`.
- **Gotchas:** `sentinel.config.json` es el contrato futuro canónico; `.quality/guard-policy.json`, `quality.config.json` y `npm run task:check` quedan como compatibilidad de migración. No se implementó el runtime ni se renombraron todavía los scripts existentes.
- **Sentinel:** el plan incluye el contrato de analizadores, paridad CLI/LSP y la retirada de controles duplicados.
- **GLORY:** no aplica; es arquitectura agnóstica del tooling.

## 028A-11 — Auditoría profunda del plan Sentinel/VarSense

- **Qué:** se revisaron las capacidades reales de Sentinel `0.4.x`, VarSense `2.2.x` y el orquestador actual. El plan ahora distingue `analyze` de `check`, define el contrato de analyzer, la migración v1→v2 de configuración, `sentinel.lock.json`, leases para procesos hijos, límites reales de shims y la estrategia observe→enforce.
- **Archivos:** `Agente/planes/plan-global-quality-guard-agnostico-2026-08-02.md`, `Agente/planes/plan-optimizacion-sentinel-varsense-2026-08-02.md`, `roadmap-sentinel.md`.
- **Gotchas:** el CLI actual de Sentinel todavía solo ofrece `analyze`; `check`, `guard` y `doctor` son fases futuras. `sentinel.config.json` ya existe con esquema v1 de análisis, por lo que no se puede ampliar silenciosamente sin migración/rollback.
- **Sentinel:** se añadieron contratos de protocolo, estados de error, versionado de runtime/analyzers y pruebas de enforcement multi-shell.
- **GLORY:** no aplica; es arquitectura agnóstica de herramientas.

## 028A-12 — Probe inocuo de verificación del interceptor

- **Qué:** se añadió el comando deliberadamente inexistente `npm run __sentinel_guard_probe__` a la lista de comandos bloqueados. No se añadió al `package.json`: con el guard activo devuelve `BLOQUEADO` antes de invocar npm; sin interceptor solo produce el error rápido de script inexistente, permitiendo verificar la shell sin consumir recursos.
- **Archivos:** `scripts/quality/quality-command-guard.mjs`, `scripts/quality/tests/quality-command-guard.test.mjs`, `roadmap-sentinel.md`.
- **Gotchas:** el probe no demuestra que Cargo/Rust esté bloqueado; demuestra únicamente si la shell/launcher está cargando el guard global. Las shells ya abiertas deben reiniciarse para probarlo correctamente.
- **Sentinel:** fixture explícito para detectar perfiles Bash/PowerShell/launcher sin interceptor.
- **GLORY:** no aplica; es tooling agnóstico.

## 028A-17 — Botones del consentimiento visibles en dark mode

- **Qué:** el banner `.analyticsConsent` es una superficie del OS (`--sistema-*`) que vive fuera de `.desktop-window`/`.movilApp`, donde el override del dark mode redefine `--color-*` a blanco. Sus botones `.boton` (receta compartida) forzaban `color: var(--color-texto)` (negro en `:root`), quedando invisibles sobre la superficie oscura. Se añadió `.analyticsConsent .boton { color: var(--sistema-texto); }` (+ variante `:disabled` con `--sistema-texto-deshabilitado`): el token del sistema se auto-invierte en `[data-tema='oscuro']`, igual que el resto del banner.
- **Archivos:** `frontend/src/styles/components.css`, `roadmap.md`.
- **Verificación real (navegador):** en dark mode (`data-tema=oscuro`) el banner tiene fondo `rgb(0,0,0)` y los botones "Permitir"/"Rechazar" `rgb(255,255,255)` (antes negros sobre negro). En light mode fondo `rgb(255,255,255)` y botones `rgb(0,0,0)` — sin regresión. Estado de sesión del navegador restaurado (consent `granted`, tema `oscuro`).
- **Gotchas:** el gate exige que la tarea exista en roadmap/planes/completados antes de correr (`SETUP ERROR` si no); se añadió la entrada al roadmap antes del gate. En light mode `--sistema-texto` = `#000000`, idéntico a `--color-texto`, así que no hay regresión posible.
- **Sentinel:** no aplica regla nueva; se consumen tokens existentes del sistema (regla 9.1).
- **GLORY:** no aplica; es un matiz de superficie del OS de wandori.us.
