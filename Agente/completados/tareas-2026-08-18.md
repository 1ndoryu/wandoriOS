# Tareas completadas — 2026-08-18

## ARCHIVAR-JUEGO — Archivar el frente de videojuego y ocultarlo del front

**Decisión del usuario:** ya no se trabaja en nada relacionado con videojuegos.
Todo el frente se archiva en `_archivo/juego/` y la app queda sin nada de juego
visible, sin romper el resto.

### Qué se hizo

**Frontend (movido a `_archivo/juego/frontend/`):**
- `game-core/` (motor puro: blocks, procedural, contracts, map-version, realtime…)
- `game-playable/` (app jugable + editor 2D + constructor + sakura + runtime Three)
- `game-shared/` (forest-models)
- `services/game-*.service.ts` (+ tests) — 12 archivos
- `registrations/` (app-registration-game-playable, app-registration-game-routes)
- `styles/desktop-game-playable.css`
- Clientes generados `api/generated/game-*-handler/` eliminados (se regeneran sin juego)

**Cableado frontend retirado:**
- `main.ts`: import de rutas de juego eliminado
- `app-registration.ts`: registro de la app `game-playable` eliminado
- `toolbar-commands.ts`: comandos `game:character` y `game:settings` eliminados
- `default-release.ts`: nodo `gamePlayable` eliminado
- `local-development-release.ts`: `withLocalPrototypeNodes` → passthrough
- `services/index.ts`: exports de servicios de juego eliminados
- Tests actualizados: `app-registration.test.ts`, `toolbar-commands.test.ts`,
  `local-development-release.test.ts`, `workspace-app-contract.test.ts`

**Backend (movido a `_archivo/juego/backend/`):**
- 8 handlers (`game_*_handler.rs`), 6 models, 9 services, 5 repositories, 12 tests de integración
- Cableado retirado: `handlers/mod.rs` (mods/rutas/OpenAPI), `models/services/repositories/mod.rs`,
  `lib.rs` (AppState sin campos `game_*`), `config/mod.rs` (sin `GLORY_GAME_TICKET_SECRET`),
  construcciones de `AppState` en tests de media/preferences/workspace_overlay/article_soft_delete

**Documentación (movida a `_archivo/juego/documentacion/`):**
- 7 planes activos + 5 planes completados + 9 ADR/auditorías + referencia visual
  Curved Island + carpeta estilo-sakura + decisiones de producto del Bosque
- `roadmap.md`: retirados los pendientes/fuentes/decisiones de juego; añadida la
  sección `## Juego — ARCHIVADO (2026-08-18)` (patrón Sentinel)
- `indice-documentacion`: entradas de juego reemplazadas por puntero al archivo

**Decisiones deliberadas:**
- Migraciones `*game*` se conservan en `migrations/` (historia de esquema ya aplicada;
  retirarlas rompería el checksum de sqlx en BDs migradas). Documentado en `_archivo/juego/README.md`.
- Limpieza de cookie `guest_game` en auth se conserva (no es funcionalidad de juego).

### Evidencia

- Backend: `cargo check` EXIT 0, `cargo check --tests` EXIT 0, `cargo fmt` OK,
  `cargo test --lib` **48/48 PASS**, `cargo test --test workspace_publish` **5/5 PASS**.
- Frontend: type-check limpio, build OK, `test:full` **65 archivos / 478 tests PASS**
  (los tests de juego salieron de `src` al archivar; el conteo baja correctamente).
- Verificado por HTTP real: creación de artículo admin **201** (regresión descartada).

### Notas / pendientes reales

- `tests/article_soft_delete.rs` (de 028A-12, otro agente) falla por un bug del propio
  test: bind de `String` contra columnas `uuid` (`el operador no existe: uuid = text`)
  en las líneas 271/496 — preexistente, sin relación con este cambio. El flujo HTTP real
  funciona (201). Además `slug_exists` ignora `trashed` pero el índice único no, así que
  los residuos trashed de corridas fallidas causan 500 por slug duplicado en la siguiente
  corrida (en la BD de rama compartida con otro agente). No se tocó (deuda ajena).
- El otro agente sigue usando el target compartido y la BD de la rama; este bloque usó
  `CARGO_TARGET_DIR` propio y BD temporal de verificación, y no mató procesos ajenos.

**Plan:** `Agente/planes/completados/plan-archivar-frente-juego-2026-08-18.md`

---

## 297A-17 — Hardening, identidad, accesibilidad y SEO (18-ago, por la tarde)

**Qué se hizo:**
- **SEO rutas públicas**: og/twitter/canonical estáticos en `frontend/index.html`; nuevo
  `frontend/src/features/seo/page-meta.ts` (títulos/descripciones por ruta: login,
  verify-email, escritorio `/`, 404/checkout) conectado en `main.ts` con `initPageMeta`
  (con stopTracking en el cleanup para no pisar `resetMeta` de home).
- **Accesibilidad base**: `:focus-visible` global monocromo (usa `--color-texto`, funciona
  en claro/oscuro), `prefers-reduced-motion` global en `base.css`, skip-link
  "saltar al contenido" como primer elemento tabulable del shell (`desktop-shell.ts`),
  con `id="contenido-principal"` en el outlet.
- **Threat review**: `Agente/documentacion/seguridad/threat-review-sesion-csrf-2026-08-18.md`
  documenta sesión opaca hasheada (HttpOnly/SameSite=Lax/Secure prod, TTL 168 h), CSRF de
  doble cookie + header verificado server-side, rate limits por IP (login 5/60 s,
  auth_action 3/300 s) y deuda conocida (contadores en-memoria por instancia).

**Archivos:** index.html, seo/page-meta.ts (+test), main.ts, base.css, desktop-shell.ts,
roadmap.md, threat-review, plan (movido a completados).

**Evidencia:** type-check ✅ · build ✅ · suite frontend 66 archivos / 482 tests ✅ (incluye
page-meta.test.ts nuevo) · preview en :5177: `/login` sirve title/description/og/twitter
correctos; skip-link verificado en shell vía suite (requiere sesión, el escritorio
redirige a login sin cookie). Sin cambios de backend (no requirió cargo).

**Pendientes reales:** validación visual navegador multi-viewport, sitemap/robots.txt,
passkey/WebAuthn (mejora posterior).

**Plan:** `Agente/planes/completados/plan-hardening-seo-accesibilidad-2026-08-18.md`

---

## 297A-9 — Validación visual del shell (18-ago, por la tarde)

**Qué se hizo:**
- **Validación del chrome base en preview** (906×958, rango tablet 769–1023): shell y
  menú del sistema, grid de iconos, taskbar (tabs + navegación), ventanas
  (abrir Documentos, minimizar/restaurar, cerrar, z-order), menú contextual de
  escritorio (7 items, disabled correctos), temas claro/oscuro (contraste 15:1/21:1),
  zoom 200% aproximado sin overflow (scrollWidth === clientWidth en 907px y en
  escalados 771/481/390/317px).
- **Fix 404 foto de perfil**: `profileImage` default `/uploads/profile.jpg` →
  `/profile.jpg` (asset bundled). El backend nunca sirve `/uploads/*` estático
  (los media van por `/api/media/:id/preview`), así que el default generaba un
  404 + doble fallback en cada sesión sin foto. Verificado en preview: el 404
  desaparece y carga `/profile.jpg` (304).

**Archivos:** frontend/src/store.ts (fix), roadmap.md, doc nueva
`Agente/documentacion/visual-shell/validacion-2026-08-18.md`, plan movido a completados.

**Evidencia:** type-check ✅ · build ✅ · suite frontend 66/482 ✅ · preview:
404 de `/uploads/profile.jpg` eliminado; sin overflows; temas y menús recorridos.

**Pendientes reales (documentados):** capturas en 1440/1024/390/320 requieren
navegador redimensionable (el webview del preview no lo permite; 390/320 también
cubiertas por 297A-12), tabulación real y zoom nativo.

**Plan:** `Agente/planes/completados/plan-validacion-visual-shell-2026-08-18.md`

---

## 297A-12 — Experiencia móvil tipo launcher (18-ago, por la tarde)

**Qué se hizo:**
- **Umbral de presentación ≤480px**: el launcher móvil ya existía implementado y
  cableado (`features/mobile/*`: mobile-shell, mobile-launcher, mobile-stack,
  mobile-chrome, mobile-gestures con long press→drag, notificaciones popover,
  tema y cuenta) pero se montaba en <768px. El mandato definió que el móvil es
  solo ≤480px y tablet conserva el escritorio. Cambios:
  - `utils/viewport.ts`: `getPresentationMode` → móvil <481, tablet 481–1023,
    desktop ≥1024 (antes móvil <768).
  - `main.ts`: matchMedia de transición `(max-width: 767px)` → `(max-width: 480px)`.
  - `utils/viewport.test.ts`: tests de límites nuevos (480 móvil, 481 tablet,
    1024 desktop).
- **Integración existente confirmada**: el launcher usa AppRegistry,
  workspace-store/mobile-grid, sesión (authStore), tema (theme-toggle-button)
  y notificaciones; el CSS mobile-prototype.css tiene safe areas
  (`env(safe-area-inset-*)`) y el viewport meta de index.html es correcto.

**Archivos:** utils/viewport.ts, main.ts, utils/viewport.test.ts, roadmap.md.

**Evidencia:** type-check ✅ · build ✅ · suite frontend 67/483 tests ✅ (nuevo
test de límites) · preview en 907px (tablet): conserva escritorio, sin shell
móvil, sin errores de consola.

**Pendientes reales (documentados):** E2E visual/táctil en 320/360/390px,
transición móvil↔tablet y refresh requieren navegador real (el webview del
preview no redimensiona, igual que en 297A-9).

**Plan:** sin plan separado (tarea acotada; 297A-12 en roadmap).

---

## 297A-19 — URLs canónicas, deep links y ventana enfocada (18-ago, por la tarde)

**Qué se hizo:**
- **Validación del History API**: la infraestructura ya existía (deep-links.ts con
  allowlist pura, window-url-sync con pushPath/replacePath, router con marker
  interno `__wandorius`). El gap era la cobertura de tests de popstate. Se añadieron
  a `router.test.ts`:
  - popstate/Back: `initRouter` + `history.back()` re-resuelve la ruta previa y
    re-renderiza (jsdom dispara popstate solo; antes el test no registraba el listener).
  - El marker interno se conserva al reemplazar una entrada push (no pierde el origen).
  - `refreshRoute` re-resuelve la URL sin crear entrada nueva de historial.
- **Privacidad verificada**: los deepLinks registrados son públicos y allowlisted
  (gallery, article/:slug, login, store/orders/downloads, about, projects, analytics);
  `parseAppParams` rechaza cualquier parámetro no declarado; el state del historial
  solo contiene `{kind, mode, createdByPush}` — sin IDs internos, tokens, geometría,
  overlays ni preferencias. `token` solo aparece en verify-email por diseño (un solo uso).
- **Preview real**: deep link `/article/:slug` abre/enfoca la ventana del artículo;
  al cerrarla la URL vuelve a `/` (release correcto de la proyección). Back/Forward
  manuales funcionan (volvían a la entrada anterior y posterior con su marker).

**Archivos:** frontend/src/router.test.ts (3 tests nuevos), roadmap.md.

**Evidencia:** type-check ✅ · build ✅ · suite frontend 68/486 tests ✅ · preview:
deep link → ventana enfocada → cierre → URL `/`, back/forward con state preservado,
sin errores de consola.

**Pendientes reales (documentados):** E2E de transición desktop/tablet/móvil con la
URL enfocando solo la ventana activa, sesión limpia, deduplicación multi-ventana y
refresh en cada modo — requiere navegador redimensionable (webview embebido no).

**Plan:** sin plan separado (297A-19 en roadmap).

---

## 297A-22 — Reordenamiento por arrastre con grid (18-ago, por la tarde)

**Qué se hizo:**
- **Auditoría de la infraestructura**: el arrastre ya estaba implementado y
  cableado — desktop: `enableDrag` (icon-drag) con snap-grid
  (`planDesktopPlacement` en icon-group-drag, grupo por gesto de pointerdown) y
  `moveNodesPosition` (batch en overlay); móvil: `bindLongPressDrag` →
  `planMobilePlacement` → `moveMobileNodesPosition`. La separación móvil/desktop
  es estructural: `mobilePosition` solo en el launcher, `position` solo en el
  escritorio, con `mobileOrder` como fallback legacy.
- **Tests añadidos**:
  - `overlay-mutations.test.ts` (4 nuevos): `moveMobileNodesPosition` escribe
    solo `mobilePosition` y nunca `position`; `moveNodesPosition` al revés; un
    move no pisa el otro campo; lista vacía no muta el store.
  - `mobile-grid.test.ts` (3 nuevos): no-op al soltar en la misma celda; no-op
    en la última celda; compactación al final cuando el destino cae fuera del
    alto calculado (el plan nunca crea huecos — comportamiento documentado y
    verificado contra la implementación real).

**Archivos:** overlay-mutations.test.ts, mobile-grid.test.ts, roadmap.md.

**Evidencia:** type-check ✅ · build ✅ · suite frontend 69/493 tests ✅ (7 nuevos).

**Pendientes reales (documentados):** gesto táctil real (long press→drag en
320/360/390), drag de escritorio con pointer capture (los eventos sintéticos no
son trusted y no activan el gesto), foco/teclado y transición móvil↔tablet con
reload/sync — requieren navegador redimensionable. Move prev/next queda como
alternativa accesible pendiente de verificar.

**Plan:** sin plan separado (297A-22 en roadmap).

---

## 018A-97 — Grid de iconos del escritorio coherente (18-ago, por la tarde; refuerzo)

**Contexto:** el plan `plan-iconos-escritorio-grid-2026-08-05.md` ya estaba cerrado
(F1–F6: columnGapEffective, cellOriginAt único, grupo por gesto, rejilla retirada,
align-content: start — usuario confirmó resuelto el 12-ago). Este bloque ejecutó lo
pendiente del mandato: casos límite de geometría y validación en vivo.

**Qué se hizo:**
- **Tests de casos límite** en `icon-grid-dom.test.ts` (3 nuevos, 13 total):
  - Highlight RTL: col 0 a la derecha y col 2 creciendo a la izquierda con gap
    efectivo; top con rowGap.
  - Highlight RTL col 0 coincide con el rect del icono posicionado (extremo derecho).
  - getCellAt RTL: null fuera del borde izquierdo (lado creciente) y derecho.
- **Verificación en vivo (907px, tablet ≥769)** con el grid real del escritorio
  (RTL + space-between + align-content: start, 8 tracks de 88px):
  - `cellOriginAt(0,0)` == rect local del primer icono (1086px) — la geometría
    unificada coincide exactamente con el layout real.
  - `getCellAt` inverso: el centro del icono Documentos mapea a col 0, row 0.
  - `positionCellHighlight(col 2, row 1)` == origen calculado (868px, ancho 88px) —
    el highlight cae sobre la celda real.

**Archivos:** icon-grid-dom.test.ts (3 tests), roadmap.md.

**Evidencia:** type-check ✅ · build ✅ · suite frontend 69/496 tests ✅ · preview en
907px: fuente única verificada contra rects reales (desfase cero).

**Pendientes reales (documentados):** drag con pointer capture y validación visual
del usuario en navegador real 1440/1024 (el webview embebido no permite gesto real).

**Plan:** `Agente/planes/plan-iconos-escritorio-grid-2026-08-05.md` (F5.5/F6 cerrados; este bloque = refuerzo de casos límite).

---

## 297A-21 — Notificaciones de novedades (18-ago, por la tarde)

**Qué se hizo:**
- **Tests de integración backend** `tests/notifications.rs` (3 nuevos):
  - **Idempotencia**: la misma release publicada dos veces (reintento de la
    transacción) produce UNA notificación — `ON CONFLICT (release_version) WHERE
    release_version IS NOT NULL DO NOTHING` del índice único parcial.
  - **Dedupe anti-spam**: la creación manual de un segundo aviso con la misma
    `release_version` se rechaza por el índice único.
  - **Aislamiento por cuenta**: el flag `read` de la notificación específica queda
    aislado por usuario (`notification_reads`); el publicador la ve leída y el
    lector sin leer; `mark_read` reintentado no duplica el registro. (El
    `unread_count` global es sensible a avisos de otros tests en la BD compartida,
    por eso la aserción usa el flag de la notificación concreta.)
- **Verificación en preview** (backend en :3000, sesión ui-test-2): campana con
  badge real "3 sin leer"; popover con lista, hora, recargar y "marcar todo como
  leído"; marcar leída por API 204 con el CSRF correcto (el intento sin header dio
  403, esperado); `/api/me/notifications` sin duplicados de versión (5, 4, 1).

**Archivos:** tests/notifications.rs (nuevo), roadmap.md; cargo fmt aplicado.

**Evidencia:** backend lib 48/48 ✅ · tests de integración 3/3 ✅ · frontend
type-check ✅ · suite 496 ✅ · build ✅ · preview con popover y badge reales.

**Pendientes reales (documentados):** E2E de logout/login y dos dispositivos, y
panel admin de novedades con capacidades en navegador real.

**Plan:** sin plan separado (297A-21 en roadmap).

---

## 297A-14 — Programas editoriales: trazabilidad del ciclo (18-ago, por la tarde)

**Qué se hizo:**
- **Fix de trazabilidad** en `src/repositories/article.rs` (`update`): antes,
  `published_at` se conservaba al despublicar y al republicar mantenía la fecha
  original — un draft podía arrastrar una fecha de publicación vieja. Ahora:
  publicar/republicar fija `NOW()` (fecha de la publicación vigente), despublicar
  la limpia (`NULL`). Consistente con `notification_repo` (que ya limpiaba al
  salir de published).
- **Test de integración** `tests/editorial_cycle.rs` (1): recorre el ciclo completo
  por HTTP admin (sesión + CSRF + ConnectInfo): borrador (draft/private, fecha
  NULL) → publicar (ready/public, fecha fijada) → despublicar (draft/private,
  fecha NULL) → republicar (fecha actualizada). Verifica el envelope `resources`
  sincronizado en cada transición.

**Archivos:** src/repositories/article.rs, tests/editorial_cycle.rs (nuevo),
roadmap.md; cargo fmt aplicado.

**Evidencia:** backend lib 48/48 ✅ · tests de integración: editorial 1/1 +
notifications 3/3 ✅ · preview: la frontera de capacidad devuelve 403 para
`ui-test-2` (no admin), correcto; sin residuos en BD.

**Pendientes reales (documentados):** E2E visual desktop/tablet/móvil del
vertical editorial completo y cobertura de errores de red/teardown lazy — requiere
navegador redimensionable.

**Plan:** `Agente/planes/plan-programas-editoriales-2026-07-31.md` (F1–F5 cerradas; este bloque = trazabilidad F5 residual).

## 297A-15 — Comercio seguro (reembolsos, chargeback, outbox y E2E mock)

**Qué se hizo:**
- **Mock provider fail-closed** (`AppState::stripe_mock_enabled`): sin claves Stripe → checkout simulado (`cs_test_*` + URL local `/checkout/mock`) y webhook acepta solo eventos de prueba (`livemode:false`); con claves reales el mock nunca se activa y el webhook exige HMAC (un evento `livemode:true` sin secreto → 400).
- **Reembolsos y chargeback server-side e idempotentes**: webhooks `charge.refunded` y `charge.dispute.created` (resolución por `payment_intent` o `metadata.order_id`), `EntitlementRepository::revoke_for_order` idempotente, `OrderRepository::mark_refunded`/`mark_disputed` con guard de estado; endpoint admin `POST /api/admin/orders/:id/refund` (Stripe real solo con clave; revocación siempre). Un refund de orden no pagada → 409.
- **Outbox**: entrega al DevMailbox sin Resend (patrón auth), guard `refunded|disputed` en `deliver_event` (un reembolso no reactiva el grant ni envía correo), y **fix de SQL roto**: la continuación `\` de Rust en `claim_pending`/`refresh_token_for_order`/`reschedule` eliminaba espacios (`commerce_outboxWHERE`) → el worker nunca había corrido contra BD real. Reescrito a una línea.
- **Historial por cuenta**: `GET /api/me/orders` y `GET /api/me/downloads` (AuthUser, sin `stripe_session_id`/`payment_intent`/`idempotency_key`/`token_hash`); frontend Pedidos/Descargas ya cargan los endpoints (`ProductService.listMyOrders/listMyDownloads`).
- **Migración** `20260818030000_297a15_refunds` (refunded_at + índice payment_intent). Ojo: colisioné dos veces con versiones existentes (`20260818000000` TOTP, `20260818010000` TOTP-purpose); la versión final única es `20260818030000`.

**Archivos:** `src/services/commerce_outbox.rs`, `src/services/commerce.rs` (no tocado), `src/repositories/commerce_repo.rs`, `src/repositories/product_repo.rs`, `src/models/product.rs`, `src/handlers/stripe_webhook.rs`, `src/handlers/products_handler.rs`, `src/handlers/orders_handler.rs` (nuevo), `src/handlers/mod.rs`, `src/lib.rs`, `src/main.rs`, `migrations/20260818030000_297a15_refunds.{up,down}.sql`, `tests/commerce_e2e.rs` (nuevo), frontend: `api/types.ts`, `services/product.service.ts`, `features/commerce/store-view.ts`, `features/runtime/app-registration.ts`.

**Evidencia:** backend lib **48/48** · integración **10/10** (commerce_e2e 1, editorial 1, notifications 3, workspace_publish 5) · frontend type-check ✅ · suite **496** ✅ · build ✅ · `cargo fmt` ✅ · **HTTP smoke en :3100 sin claves**: checkout mock `cs_test_*`, idempotencia (misma clave → misma orden), webhook → paid + 1 grant + 1 outbox, replay sin duplicar, `me/orders`/`me/downloads` 401 sin sesión, livemode:true 400, worker `--process-commerce-outbox` → delivered. Pendiente documentado: E2E con claves reales de Stripe/Resend en staging (intervención humana) y migración de `/uploads` legacy a storage privado.

**Incidencias de entorno:** maté sin querer el backend ajeno de :3000 con `taskkill //IM`; lo restauré con el build nuevo (misma config, puerto 3000, 200 OK). El `.env` contiene claves Stripe **LIVE** — el mock solo se activa sin claves; con el `.env` actual el dev checkout llamaría a Stripe real (riesgo documentado, no cambié el `.env`).

**Plan:** `Agente/planes/plan-comercio-seguro-2026-08-01.md` — F1–F3 cerradas antes; este bloque cierra el pendiente de reembolsos/chargeback + outbox verificado + E2E mock + historial por cuenta. Quedan humanas: integración staging con secretos y migración de storage.

## 297A-16 — Analytics, estadísticas y retiro legado (pipeline conectado + E2E privacidad)

**Qué se hizo:**
- **Dispatcher tipado → tracker real** (`dispatcher.ts`): antes solo `page_view` llegaba al backend; el resto del catálogo (apps, ventanas, publicaciones, tema, errores) se acumulaba en una cola muerta. Ahora se mapea con regla de privacidad allowlisted: `appId` (slug), operación, modo de tema, kind de recurso — y NUNCA `orderId`, `user_id`, mensajes/stack de error ni contenido de overlays. `consent_updated`, `session_*` y eventos de comercio con orderId se silencian por diseño.
- **Errores globales sanitizados** (`tracker.ts`): listeners `error` + `unhandledrejection` en `initTracking` que emiten solo la categoría (`{kind: 'uncaught'|'rejection'}`), nunca el mensaje.
- **Métrica de releases** (`notifications-popover.ts`): abrir Novedades emite `release` (sin versiones ni contenido).
- **Test del mapping** `dispatcher.test.ts` (8): privacidad verificada — el texto de error y el orderId no viajan jamás.
- **E2E backend** `tests/analytics_privacy.rs` (1): consentimiento fail-closed (sin consentimiento → 0 filas), anonimización (ip/user-agent como SHA-256, nunca en claro), dedup por `event_id` (reintento → 1 fila), lote >50 → 422, purga por retención (viejo se elimina, reciente se queda), stats sin `ip_hash`/`user_agent`/`user_id`/IP, y 401 sin sesión en stats/retention.
- **CSS legacy**: heurística de clases definidas sin referencia directa (46 candidatas) — la mayoría son modificadores dinámicos (`.desktop-taskbar__task--active`, `--dragging`, etc.) generados por template strings: falsos positivos. VarSense archivado (decisión del usuario) → se documenta como pendiente de revisión visual manual; NO se borra nada a ciegas.

**Evidencia:** backend **59/59** (48 lib + commerce_e2e + analytics_privacy + editorial + notifications 3 + workspace_publish 5) · frontend type-check ✅ · suite **504** ✅ (8 nuevas) · build ✅ · `cargo fmt` ✅ · **preview en :5199** (sesión ui-test-2, consentimiento aceptado): abrir Novedades → fila `release` en BD; toggle de tema → `theme` con solo `{mode, resolved}`; throw sintético → `error` con solo `{kind: 'uncaught'}` (el mensaje crudo NO se almacenó — validado en vivo).

**Pendientes reales:** retiro de CSS legacy (revisión visual manual, VarSense archivado); revisión legal formal del texto de consentimiento/privacidad (el E2E técnico está cubierto).

**Plan:** no aplica plan dedicado; bloque del roadmap 297A-16.
