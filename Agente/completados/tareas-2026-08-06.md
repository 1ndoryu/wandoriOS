# Tareas completadas — 2026-08-06

## SNT-15 — Endurecimiento del slice local de adapter

### Qué
Se cerró el siguiente bloque reversible de la migración sin tocar Sentinel upstream, retirar scripts ni modificar la skill global. El manifest local es ahora una frontera estricta y físicamente contenida: gobierna la selección/paridad de stages, rechaza schema incompleto o claves desconocidas, no permite symlink/junction escape y mantiene una allowlist portable sin credenciales.

### Archivos
- `scripts/quality/adapter-manifest.mjs`
- `scripts/quality/stage-definitions.mjs`
- `scripts/quality/tests/adapter-manifest.test.mjs`
- `scripts/quality/tests/stage-definitions.test.mjs`
- `Agente/planes/plan-migracion-scripts-adapters-sentinel-2026-08-06.md`
- `Agente/documentacion/herramientas/inventario-scripts-adapters-sentinel-2026-08-06.md`
- `Agente/completados/tareas-2026-08-06.md`

### Gotchas
- `manifestStageNames`, `adapterStageNames` y `assertImplementedStages` validan el manifest completo antes de usar stages/perfiles.
- Cada definición de stage solo acepta `timeoutMs`; los typos fallan cerrado.
- `quality-adapter.json` y su entrypoint deben ser archivos regulares contenidos en el workspace; symlink/junction y realpaths fuera se rechazan, incluso en la ruta síncrona usada por `task-check`.
- La allowlist efectiva deduplica case-insensitive para Windows (`PATH`/`Path`) y rechaza nombres sensibles tanto en la declaración como en la allowlist base heredada.
- No se retiró ningún script ni se publicó ningún cambio upstream: faltan schema upstream, fixtures independientes, paridad multi-proyecto y rollback.

### Sentinel / GLORY
- `node --check scripts/quality/adapter-manifest.mjs`: PASS.
- `node --check scripts/quality/stage-definitions.mjs`: PASS.
- Suite focalizada final: **19/19 PASS, 0 FAIL**.
- `git diff --check`: PASS.
- `tools/sentinel` y `tools/varsense` quedaron en los gitlinks fijados, sin cambios de contenido.
- `glory-rs` conserva el gitlink histórico `ec33d520`; el objeto no está disponible en el remoto actual, por lo que no se sustituye ni se fuerza su checkout.
- No se declara gate completo ni observe end-to-end: el worktree no contiene `.env`/`DATABASE_URL`, `frontend/node_modules` ni CLI compilado de VarSense.
- Skill global: no modificada; el bloque no aporta todavía evidencia para una regla global o publicación upstream.

### Pendiente
Schema upstream del manifest, fixtures Node/Vite y Rust/PostgreSQL independientes, paridad CLI/LSP/editor, cinco gates reales, rollback/GC y dos releases consecutivos antes de retirar duplicaciones.

## SNT-16 — Contrato upstream de manifests de stages (bloqueado para adopción)

### Qué
El diseño y la integración upstream se exploraron en un worktree temporal, pero no existe actualmente un commit recuperable/publicado en el submódulo consumidor. El consumidor permanece fijado en Sentinel `20c13a2`; no se cambió el gitlink, lock ni skill.

### Límite
No se declara compilación ni suite upstream PASS. El entorno local carece del helper esperado por el guard (`quality-command-guard.mjs`) y del provisionamiento necesario para una validación reproducible. El hash `ef9c751` no existe en los objetos/referencias recuperables consultados.

### Rollback
Conservar Sentinel 0.5.0 y `task-check`/adapter local; no retirar duplicaciones.

## SNT-16b — Harness local de fixtures y paridad (preparado, no adoptado)

### Qué
Se añadió un harness local y reversible para fijar la identidad completa de un hallazgo antes de ejecutar el contrato upstream: decisión/estado, `ruleId`, severidad, archivo, línea y mensaje. Los JSON representan dos productores agnósticos (Node y Rust) con el mismo hallazgo y una regresión negativa que cambia severidad/mensaje.

### Archivos
- `scripts/quality/tests/fixtures/snt-16b-node-report.json`
- `scripts/quality/tests/fixtures/snt-16b-rust-report.json`
- `scripts/quality/tests/snt-16b-parity.test.mjs`
- `Agente/planes/plan-migracion-scripts-adapters-sentinel-2026-08-06.md`
- `Agente/documentacion/herramientas/inventario-scripts-adapters-sentinel-2026-08-06.md`
- `roadmap.md`

### Evidencia
- `node --test scripts/quality/tests/snt-16b-parity.test.mjs`: **2/2 PASS**.
- `git diff --check`: PASS.
- El harness es deliberadamente estático: **no** ejecuta todavía envelope vs legacy mediante Sentinel, ni demuestra paridad CLI/core/LSP/editor o multi-shell/CI.
- Suite amplia del consumidor: **223 PASS, 3 fallos de entorno, 1 skip**. Requiere CLI Sentinel/VarSense y configuración local provisionada; no se degradó ningún error a PASS.

### Decisión de scripts y skill

No se eliminó ni migró físicamente ningún script. `task-check`, adapters, `run-with-db`, codegen y preparación de BD permanecen según su responsabilidad. La skill global no se actualizó: la regla candidata (manifest versionado, paths físicos contenidos y fail-closed) requiere release upstream, ejecución mediante el CLI real y confirmación en sesión nueva.

### Siguiente bloque

Publicar/recuperar el contrato upstream en el repositorio autorizado, compilar desde clon limpio, crear dos proyectos independientes ejecutables, correr envelope y legacy, comparar paridad multi-shell/CLI/LSP/editor, fijar locks y solo después considerar SNT-17.
