# Tareas completadas — 2026-08-10

## 108A-6 — Retirada Legacy (bloque de adopción posterior a la auditoría)

**Contexto:** grupo de tareas pendientes de la adopción del release Sentinel tras la auditoría
108A-1: retirar el stage `custom`, adoptar el segundo consumidor, publicar correctamente el
release, integrar `sentinel check --stages` como gate canónico y retirar la duplicación legacy
condicionada a dos releases con rollback.

### Entregado

1. **Stage `custom` retirado**: en wandorius (commit `2244eee7`) y en glory-rs-rest (commit
   `f13d0e16`, mismo cambio): `scripts/quality/custom-rules.mjs`,
   `scripts/quality/adapters/custom.mjs` y `scripts/quality/tests/custom-rules.test.mjs`
   eliminados; `profile-contract.mjs`, `stage-definitions.mjs`, `quality-adapter.json` y los
   tests que referenciaban `custom` actualizados. En glory-rs-rest la edición se aplicó por
   terminal (las herramientas de archivo del cliente devuelven `[BLOCKED]` en
   `glory-rs-rest/scripts/quality/*`), con JSON validado y suite verde. Suites: wandorius
   240 pass / 1 skip / 0 fail; glory-rs-rest 231 pass / 1 skip / 0 fail.

2. **Segundo consumidor adoptado**: `glory-rs-rest` con pin `c1f8f1f`/0.6.0 (commit `a67b25e7`)
   y luego re-pinado a la release 0.7.0 (`a804c0d`/0.7.0) con lock regenerado y doctor PASS.

3. **Release 0.7.0 publicada correctamente**: merge de `f1/cli-contracts` (auditoría 108A-1) sobre
   `main` 0.6.4 en `.sentinel-upstream-f1` (worktree exclusivo) → commit `a804c0d`, publicado en
   `origin/main` + tag `v0.7.0`. El pin anterior (`c1f8f1f`) vivía solo en la rama de feature y no
   era alcanzable desde `origin/main`/`v*`, por lo que el preflight de release del Core
   (`tool-release-unpublished`) bloqueaba `sentinel check --stages`.

   - Conflictos de merge resueltos en `src/cli/index.ts` (barril del split CLI) y
     `src/core/taskCoordinator.ts` (helpers `canonicalPath`/`isStrictlyInside` ya existían en
     `./pathSafety` de main; se conservó la verificación de ownership del lock en `finally`).
   - Se portaron al CLI dividido las opciones de main perdidas por el split: `--worktrees-root`,
     `--env-manifest`, `--summary`/`--plan`/`--related-task` y `task status --all`.
   - Budget de `taskCoordinator.ts` subió a 1250 (el merge absorbió env-manifest/worktrees/historia;
     la división física queda en la consolidación).
   - Gate upstream: compile OK, lint 0 errores (12 warnings preexistentes documentados),
     `test:unit` **557 passing / 1 pending**, `check:core` OK.

4. **Doble vía real demostrada**: `node scripts/quality/observe-compare.mjs --task-id 108A-1
   --profile docs` y `--task-id 297A-78 --profile docs` → **decisión y hallazgos idénticos**
   entre `task:check` (legacy) y `sentinel check --stages` (Core). Fijado además el contrato del
   manifest: `stages.mjs` ya no emite `envAllowlist` (el Core solo acepta
   name/executable/args/reportPath/expectedSchemaVersion/timeoutMs/cwd y su allowlist fija cubre
   la del adapter).

5. **Gate canónico integrado**: `scripts/quality/gate-check.mjs` (npm `gate:check`) genera el
   manifest declarativo (`stages.mjs` + `quality-adapter.json`) y delega en
   `sentinel check --stages`, emitiendo `metrics.json` con el schema histórico; CI (`quality.yml`)
   ejecuta `gate:check --ci`; `export-ci-metrics.mjs` agrega el namespace `.quality-reports/check/`.

### Pendiente (condicionado, no bloqueante)

- Retirada física de la capa A (shims del repo: `npm/npx/cargo/node.cmd`,
  `global-cargo-guard.ps1`, `global-quality-guard.sh`, `install-global-guard.ps1`,
  `quality-command-guard.mjs` + test) y de la capa B (orquestador local `task-check.mjs`,
  `heavy-run-guard.mjs`, políticas/runner/scope/cache/reporter con equivalente en el Core) cuando
  **dos releases consecutivas** en verde permitan rollback (runbook
  `Agente/documentacion/herramientas/runbook-retirada-wrappers-sentinel-2026-08-05.md` §3).
  La release 0.7.0 es la primera.

### Evidencia

- Commits: upstream `a804c0d` (release 0.7.0, main + tag `v0.7.0`); consumidor wandorius
  `79ca6507` (repin) + `18a47450` (manifest contract) + bloque gate; consumidor glory-rs-rest
  `5b542161` + `01e10d14`.
- Gates: upstream `test:unit` 557 passing; consumidor `quality:test` 240 pass; `quality:lock
  --check` pass; doctor PASS; `gate:check` PASS (108A-1, 297A-78).
- Limitación registrada: sin `--scope-manifest`, el scope del stage `sentinel` lo re-detecta el
  proceso de etapa y puede divergir del alcance del gate en el detalle de findings (no en la
  decisión); la paridad de hallazgos se verifica con `observe-compare` sobre el mismo manifest.
