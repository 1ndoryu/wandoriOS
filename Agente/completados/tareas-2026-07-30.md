# Tareas completadas — 2026-07-30

## 297A-11 — Workspace público y overlay invitado (parcial)

### Auditoría arquitectónica profunda

- Auditoría de arquitectura y escalabilidad del frontend (~15,000 líneas).
- 3 violaciones de tamaño identificadas (command-registration 725, workspace-store 430, desktop-shell 419).
- 5 problemas de escalabilidad (circular deps, store sin event typing, CSS sin layers, sin lazy loading, CommandRegistry sin namespaces).
- 1 falla arquitectónica crítica: Finder era un preview hardcodeado, no un file browser.
- Gap de modelo de datos: `WorkspaceNodeType` faltaba `'resource'` (manual §6.2 define 4 tipos, implementación solo 3).
- `ResourceTypeRegistry` existía pero ningún componente lo usaba.
- Pipeline roto: backend→workspace→Finder no existía.

**Archivos:** `Agente/documentacion/arquitectura/auditoria-arquitectura-frontend-2026-07-30.md`, `Agente/planes/plan-refactorizacion-arquitectura-2026-07-30.md`

### Fase 0: Finder como file browser real + modelo de datos

- `WorkspaceNodeType` incluye `'resource'` + `WorkspaceResourceKind` + `resourceKind` field. (`types.ts`)
- `RenderContext.params` para parámetros de instancia. (`lifecycle.ts`)
- `WindowEntry.params/_paramKey` + `openWindow()` con params y titleOverride. (`window-manager.ts`)
- `openAppWindow(appId, params?)` con dedup non-singleton via `_paramKey`. (`route-app-adapter.ts`)
- Finder reescrito completo: lee `workspaceStore`, breadcrumb, drag/drop, context menu, `ResourceTypeRegistry`. (`finder-preview.ts`)
- Finder `singleton: false`. (`app-registration.ts`)
- Carpetas abren con `folderId`, recursos resueltos via `resolveResourceType`. (`desktop-shell.ts`, `workspace-icon-grid.ts`)
- Gallery cambiado de `type: 'app'` a `type: 'folder'` en default-release.ts.
- Título dinámico de ventana Finder por nombre de carpeta.
- Validación de nombres duplicados en `createFolder()`.

**Commits:** `c1ef69fe`, `f1612d78`

### Fase 1: Split command-registration.ts (725 → 6 módulos)

- `commands/window-commands.ts` — close, minimize, restore, focus, focus-next
- `commands/geometry-commands.ts` — move/resize por teclado
- `commands/app-commands.ts` — app:open, app:focus
- `commands/workspace-commands.ts` — trash, restore, reset, clipboard, create-folder
- `commands/toolbar-commands.ts` — trash:restore-all, empty, finder:new-folder, projects:new
- `commands/keyboard-handler.ts` — initKeyboardShortcuts, matchesShortcut
- `commands/index.ts` — barrel re-export

**Commit:** `912b768c`

### Fase 2+3: Split workspace-store.ts (430→4) + desktop-shell.ts (419→3)

- `workspace/merge.ts` — mergeWorkspace, rebaseOverlay (algoritmo puro)
- `workspace/overlay-mutations.ts` — moveNodePosition, tombstoneNode, createFolder, getChildren...
- `workspace/clipboard.ts` — getClipboard, setClipboard, pasteFromClipboard, cycle validation
- `workspace-store.ts` — stores + persistence + API (lean, ~150 líneas)
- `workspace-icon-grid.ts` — grid reactivo + drag + context menu
- `reactive-taskbar.ts` — taskbar suscrito a windowStore
- `desktop-shell.ts` — orquestación pura (~200 líneas)

**Commit:** `08f4a37e`

### Sistema de menús unificado

- CommandRegistry como fuente única de verdad para toolbar y context menu.
- `AppDefinition.toolbar` con `ToolbarItemRef` (string command IDs).
- `createAppToolbar` lee de CommandRegistry via `resolveToolbarItem()`.
- Toolbar automático en todas las ventanas (menú "Ventana" default).
- Menús declarativos en Finder (Archivo → Nueva carpeta), Projects (Archivo → Nuevo proyecto), Trash (Archivo → Restaurar todo, Vaciar).

**Commits:** `f7787438`, `9f889a40`, `c1b51ac8`

### Fix migración workspace_releases

- Version duplicado `20260730000000` (conflicto con 297a10_resources).
- Renombrado a `20260731000000_workspace_releases`.
- Seed data actualizado: gallery `type: 'folder'`.

**Commit:** `ebc32d36`

### Otros fixes

- Folder nodes sin refId ahora abren Finder al hacer clic. (`91bb1188`)
- Context menu y menú bar superior funcionales.

### Auditoría arquitectónica v2 — Corrección de hallazgos

Auditoría profunda de segunda iteración del frontend. 28 hallazgos: 4 críticos, 3 altos, 12 medios. 3 hallazgos rebajados tras verificación. 7 omisiones añadidas.

**Commits:** `d0eb7543`, `7d49ca94`, `e437e499`, `9f42cc30`, `85b32b77`, `35af6aed`, `06760a8d`, `f91b70ec`, `e065ba1d`, `adcd53d8`

#### Fixes críticos (4/4)
- Eliminar código muerto: desktop-concept.ts + desktop-taskbar.ts (-274 líneas)
- MountedView.destroy(): onDestroy en WindowEntry, invocado en closeWindow
- Eliminar nodo Snake muerto de DEFAULT_RELEASE
- Finder navega en ventana existente (currentFolderId interno, no abre nueva por carpeta)

#### Fixes altos (3/3)
- Admin como AppRegistry app (eliminar navigate hardcoded)
- Unificar WorkspaceResourceKind con ResourceKind
- Menu Aplicaciones reactivo (re-lee workspaceStore al abrir)

#### Fixes medios (10/12)
- Fix triple recompute: debounce con queueMicrotask
- merge.ts: detección de huérfanos recursiva (while loop)
- maximizeWindow: toggleMaximizeWindow con preMaximizeBounds + doble-clic titlebar + toolbar
- Clipboard Ctrl+C/X/V: conectado a selectionStore (funciona sin targets explícitos)
- Ciclo de imports roto: stores.ts extraído de workspace-store.ts
- Grid responsive: repeat(auto-fill) en vez de repeat(2)
- Reader carga contenido real desde API (/api/articles/{slug})
- About como app autónoma (no importa página legacy)
- Window bounds via CSS custom properties (--win-x/y/w/h)
- Menu dropdown unificado: dropdown-menu.ts compartido + context-menu delega

#### Fixes medios adicionales (12/12 completados)
- **Toolbar migrado a dropdown-menu.ts** — createAppToolbar delega a openDropdownMenu, elimina toda la lógica de open/close/visibility duplicada. Separadores con flag `separator: true` en DropdownMenuItem. `aria-expanded` toggling. (`e065ba1d`)
- **Drag unificado Finder ↔ desktop** — icon-drag.ts reescrito como shared Pointer Events drag manager. Exports: enableDrag, makeDropTarget, onGlobalDrop. Finder elimina HTML5 DnD, usa Pointer Events. Desktop grid marcado como drop target. Handler global registrado una sola vez en desktop-shell.ts con self-drop guard. CSS: .desktop-icon--drop-hover, .desktop-finder__item--dragging. (`adcd53d8`)

### Auditoría arquitectónica v3 — Escalabilidad post-fixes v2

Tercera auditoría profunda del frontend. 12 hallazgos nuevos: 1 alto, 4 medios, 4 bajos, 3 informativos. 5 fixes aplicados.

**Commits:** `42b783ae`, `bb379b07`, `d64b3dfa`, `215ed075`

#### Fixes aplicados (16/16) ✅

| Fix | Severidad | Commit |
|---|---|---|
| reconcileChildren utility: reemplaza innerHTML='' por reconciliación diferencial de DOM | 🟠 Alto | `d64b3dfa` |
| workspace-icon-grid: reconciliación diferencial + resolveActivate helper | 🟠 Alto | `d64b3dfa` |
| reactive-taskbar: reconcileChildren reemplaza innerHTML, click handler lee del store | 🟡 Medio | `eb4db06a` |
| trash-preview: reconcileChildren para nodos tombstoneados | 🟡 Medio | `eb4db06a` |
| sidebar: reconcileChildren para nav items y artículos | 🟡 Medio | `eb4db06a` |
| profile: reconcileChildren para redes sociales | 🟡 Medio | `eb4db06a` |
| admin-articles/projects: innerHTML → textContent + DOM creation | 🔵 Bajo | `eb4db06a` |
| variables.css: elimina @layer overrides dead declaration | 🔵 Bajo | `eb4db06a` |
| Error boundary en AppRegistry.instantiate: fallback visual si app lanza | 🔵 Bajo | `215ed075` |
| WeakMap para callbacks en desktop-menu-bar (reemplaza type assertions frágiles) | 🔵 Bajo | `215ed075` |
| CSS @layer components: components.css, layout.css, pages.css envueltos | 🟡 Medio | `215ed075` |
| Split window-manager.ts (314→2): window-store.ts extrae types+store+getters | 🟡 Medio | `c1aece74` |
| Split font-panel.ts (304→2): settings-repo.ts extrae persistencia | 🟡 Medio | `c1aece74` |
| Migrar apps a registerLazy(): settings, admin, projects | 🟡 Medio | `c1aece74` |
| Closures stale en reconcile de icon grid: handlers leen de store en evento | 🔵 Bajo | `c1aece74` |

## Pendiente para siguiente sesión

- [ ] `npm run task:check -- 297A-11` — quality gate.
- [ ] 297A-11 restante: draft/release/preview/publicar/rollback.
- [ ] 297A-11 restante: organizador público separado del workspace admin personal.
- [ ] Auditoría v3: path aliases para imports profundos (único pendiente de bajo riesgo).
