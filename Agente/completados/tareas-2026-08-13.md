# Tareas completadas — 2026-08-13

## 138A-1 — Motor propio + toolkit procedural del Bosque

**Contexto (decisión del usuario, 13-ago):** cambiar el enfoque del Bosque: en
lugar de pulir el terreno por bloques como destino (128A-1), construir primero
las **herramientas necesarias para crear el juego** y decidir el estilo visual
probando, no por hipótesis. Estilo Genshin-like low poly verde stylized; sin
distinción por color entre jugadores y sin indicadores; mundo único compartido
con cap 32 (decisión "A" reafirmada); 128A-1 pasa a experimento reutilizable;
sin repo separado ni SemVer (GAME-02, 12-ago).

### Qué

1. **Paquete agnóstico `frontend/src/features/game-core/procedural/`** (datos
   puros, sin Three/DOM/red): ruido/fbm determinista por seed (`noise.ts`),
   heightmap con falloff de isla y presets `smooth`/`blocks` desde la misma
   base (`heightmap.ts`), mesher de heightfield suave indexado con presupuestos
   (`heightfield-mesh.ts`), vegetación procedural con poisson disk/jitter,
   reglas por zona y presupuesto máximo (`vegetation.ts`) y mesher de
   vegetación con instancias (`vegetation-mesh.ts`). Exports públicos desde
   `game-core/index.ts`; sin imports profundos.
2. **Comparador visual** en `/forest-playable` (`game-procedural-comparator.ts`):
   mismo seed en modo `bloques` (reutiliza el mesher del experimento 128A-1 vía
   adaptador de cuantización `game-procedural-blocks.ts`, sin mover ni duplicar
   sus módulos) y modo `suave` (heightfield + vegetación del toolkit), con
   métricas de vértices/triángulos/props y frame reutilizando
   `readRendererMetrics`. Default conserva el comportamiento actual (bloques).
3. **Integración aditiva** en `game-playable-scene.ts`: comparador montado
   oculto, `setTerrainMode`/`setTerrainMetrics`/`setPick` nullable y
   `setVisible` en la isla 128A-1; sin cambiar el comportamiento por defecto.
4. **Spike documentado:** `Agente/documentacion/arquitectura/patrones-motores-open-source-2026-08-13.md`
   con patrones adoptables de Godot (FastNoiseLite/Terrain3D), Bevy y Three.js
   (ruido/fbm, falloff, meshing, LOD/culling, budgets).
5. **Hotfix acotado del transporte quality** (`scripts/quality/stages.mjs` +
   `stage-definitions.mjs`): filtra los perfiles de clasificación
   (`desktop/mobile/workspace/auth/commerce`) antes de `adapterStageNames` para
   que el gate incremental no falle fail-closed; test de regresión añadido
   (`stage-definitions.test.mjs`).

### Archivos

- `frontend/src/features/game-core/procedural/{noise,heightmap,heightfield-mesh,vegetation,vegetation-mesh}.ts` + tests (11 archivos).
- `frontend/src/features/desktop/apps/game-playable/game-procedural-blocks.ts` y `game-procedural-comparator.ts`.
- `frontend/src/features/game-core/index.ts` (export del paquete).
- `scripts/quality/{stages.mjs,stage-definitions.mjs}` + `scripts/quality/tests/stage-definitions.test.mjs`.
- `scripts/quality/tests/observe-integration.test.mjs` (corrección del test de
  integración del transporte: mismo filtro `EXECUTABLE_PROFILES`).
- `Agente/documentacion/arquitectura/patrones-motores-open-source-2026-08-13.md`.
- `Agente/planes/plan-motor-toolkit-procedural-2026-08-13.md` (→ `Agente/planes/completados/`).
- `roadmap.md` (entrada 138A-1 retirada; queda sin commitear junto a edits ajenos).

### Gotchas

- **Gate full automático:** mientras `scripts/quality/*` estaba en el diff, el
  gate detectaba alcance full y la etapa rust con DB quedó **BLOQUEADA por el
  heavy-run-guard (SNT-11)** (cooldown hasta 2026-08-13T16:16:18Z; excepción
  manual desactivada). No era fallo de código: evidencia en `.quality-reports/check/138A-1/`
  (intento full: sentinel/varsense/frontend/docs PASS, rust guard-block).
- Tras commitear el bloque (incluido el hotfix), el rerun fue **incremental y
  PASS** sin etapa rust.
- **Regresión detectada en cierre:** `quality:test` fallaba en
  `observe-integration.test.mjs` porque calculaba `expected` sin el filtro de
  perfiles de clasificación. Corregido (commit `b34b52fc`) y suite verde:
  **233 pass / 1 skip / 0 fail**.
- **Trabajo en serie sin worktree (decisión coordinada):** los archivos
  compartidos con 128A-1 (`game-playable-scene.ts`, CSS del panel, `roadmap.md`,
  `game-curved-island*.ts`) quedan **sin commitear** para no barrer hunks
  ajenos. El commit 138A-1 (`b5302a03`) no compila aislado: depende de los
  módulos 128A-1 sin publicar (`game-block-*`, `game-curved-island*`).
- `roadmap.md` contenía edits ajenos sin commitear (128A-1, cancelación
  quality 12-ago, 297A-21): la actualización de cierre se deja en el working
  tree, no se commitea el archivo completo.

### Sentinel / gate

- `npm run gate:check -- 138A-1`: **PASS** (incremental, 31 archivos) tras el
  commit `b5302a03` — sentinel PASS, varsense PASS, frontend PASS, docs PASS.
  Repetido tras `b34b52fc` y el cierre documental (retirada de la referencia al
  plan en roadmap): **PASS** final.
- Intento full previo: sentinel/varsense/frontend/docs PASS; rust BLOQUEADO por
  cooldown del guard (evidencia documentada arriba).
- Verificación base: type-check frontend limpio; tests procedural 18/18; suite
  `game-playable` 147/147; suite quality 233 PASS / 1 skip / 0 fail (verificado
  en cierre tras la corrección del test de integración).
- Sentinel 0.7.4 (`0349485c…`), VarSense 2.2.1 (`88f281f…`), rama `wandorius`.

### GLORY

- Commit explícito: `b5302a03` (bloque de implementación) + commit documental
  de cierre (plan movido a completados y completada).
 - Revisión `sentinel_inspector`: **OK CON OBSERVACIONES, ACCION: ninguna**
   (observaciones no bloqueantes: rust full pendiente por cooldown del guard;
   working tree sucio con cambios ajenos 128A-1 documentados; claim liberado
   al cierre).
- Revisión `supervisor_reviewer`: el delegado no respondió (2×120s + nudge →
   interrupt); se aplicó el respaldo local de la skill `supervisor-review` con
   veredicto **APROBADO CON RESERVAS** (hallazgos menores no bloqueantes:
   `hashHex` duplicado en `vegetation-mesh.ts`, `tooClose` O(n²) acotado por
   presupuestos, `update()` no-op por contrato unificado, filtro
   `EXECUTABLE_PROFILES` duplicado, heightfield generado 2 veces en `rebuild`,
   stats de triángulos sin props).

## 138A-2 — Toolkit procedural: árboles low-poly y césped por matas

**Contexto:** continuación del nuevo enfoque del Bosque (138A-1): el usuario
quiere **probar** el estilo Genshin-like low poly, no decidir por hipótesis.
Este bloque amplía el toolkit con generadores deterministas de árboles con
ramas y césped por matas, y conecta ese detalle al comparador de
`/forest-playable` en el modo `suave`.

### Qué

1. **`tree-mesh.ts`** (nuevo, 150 líneas): `buildTreeMeshData(seed, options)`
   en espacio local (base en y=0) — tronco cónico de 4 caras, 2 anillos × 3
   ramas rotadas con ángulo determinista, copa por clusters (cima + tips de
   ramas, default 3, rango 1..4) y paleta del toolkit; validaciones de
   escala/altura/clusters. Reusa `pushBox`/`pushQuad`/`rgb` exportados de
   `vegetation-mesh.ts`.
2. **`grass-mesh.ts`** (nuevo, 84 líneas): `buildGrassClumpMeshData(seed,
   options)` — mata de N briznas (default 7, rango 1..24) en anillo con jitter,
   cada brizna de 2 quads cruzados con doblado determinista (hash por brizna).
3. **`vegetation-lowpoly.ts`** (nuevo, 55 líneas):
   `buildLowPolyVegetationMeshData(placements, palette)` — árboles y césped con
   los nuevos generadores, rocas con la caja toon reutilizando
   `buildVegetationMeshData` (sin duplicar código); `appendTranslated` suma
   offsets y reindexa. Corrige la doble traducción de la roca (se normaliza a
   origen y se traslada una sola vez).
4. **Exports públicos** en `procedural/index.ts` (tree-mesh, grass-mesh,
   vegetation-lowpoly) y **comparador** `game-procedural-comparator.ts`:
   el modo `suave` usa `buildLowPolyVegetationMeshData`; mismas métricas de
   stats.
5. **Limpieza del hallazgo de 138A-1:** `vegetation-mesh.ts` elimina su
   `hashHex` local y pasa a importar `hash2` de `noise.ts`; `tree-mesh` y
   `grass-mesh` normalizan el seed fraccionario de placements a entero
   (`Math.floor(seed * 1_000_000)`) para que cada instancia tenga forma propia
   (los seeds reales de `placeVegetation` son `hash2` ∈ [0,1), que
   truncaban a 0 y producían árboles idénticos).

### Archivos

- `frontend/src/features/game-core/procedural/{tree-mesh,grass-mesh,vegetation-lowpoly}.ts` + `{tree-mesh,grass-mesh,vegetation-lowpoly}.test.ts` (6 archivos nuevos).
- `frontend/src/features/game-core/procedural/index.ts` y `vegetation-mesh.ts`.
- `frontend/src/features/desktop/apps/game-playable/game-procedural-comparator.ts`.
- `Agente/planes/plan-toolkit-arboles-cesped-lowpoly-2026-08-13.md` (→ `Agente/planes/completados/`).
- `roadmap.md` (entrada 138A-2 retirada; queda sin commitear junto a edits ajenos).

### Gotchas

- **Seeds fraccionarios:** `hash2` trunca a entero de 32 bits; los seeds de
  placements viven en [0,1) y sin normalización todos los árboles/matas
  salían idénticos (los tests de determinismo lo destaparon: mismo seed ==
  misma malla, pero seed distinto también colapsaba). Se normaliza a
  `floor(seed * 1_000_000)` en los generadores.
- **Roca traducida dos veces:** `buildVegetationMeshData([p])` ya emite en
  coordenadas de mundo; se normaliza `{...p, x:0, y:0, z:0}` y `appendTranslated`
  aplica la posición una sola vez.
- **Gate primera corrida fail en docs:** el plan activo 138A-2 no tenía
  checklist (regla `docs-plan-no-checklist`); se agregó y el rerun quedó PASS.
- **Trabajo en serie sin worktree (patrón 138A-1):** el working tree sigue
  conteniendo hunks ajenos sin commitear de 128A-1/docs; el commit de
  implementación incluye solo archivos propios del bloque (sin `git add .`).

### Sentinel / gate

- `npm run gate:check -- 138A-2`: **PASS** (incremental, 41 archivos) —
  sentinel PASS, varsense PASS, frontend PASS, docs PASS. Warnings
  preexistentes de 128A-1 (clases CSS huérfanas y css-adhoc-button-style) no
  bloqueantes.
- Verificación base: `npm --prefix frontend run type-check` limpio; vitest
  procedural 32/32; suite `game-playable` 147/147.
- Sentinel 0.7.4 (`0349485c…`), VarSense 2.2.1 (`88f281f…`), rama `wandorius`.

### GLORY

- Commit explícito de implementación + commit documental de cierre (plan a
  completados y completada).
- Revisión `sentinel_inspector`: **OK CON OBSERVACIONES, ACCION: ninguna**
  (gate final PASS reproducible; claims RELEASED en ambas capas; commits en
  HEAD; observaciones no bloqueantes: PASS incremental incluye cambios ajenos
  sin commitear de 128A-1 — integración diferida; fails previos de docs solo
  quedan documentados en la completada; 7 warnings heredados de deuda técnica;
  metadata histórica ajena en `.sentinel`). No se detectó defecto real en el
  uso del gate; no aplica `sentinel_repair`.
- Revisión `supervisor_reviewer`: el delegado no respondió (3×180s + nudge →
  interrupt, mismo patrón que 138A-1); se aplicó el respaldo local de la skill
  `supervisor-review` con veredicto **APROBADO CON RESERVAS** (hallazgos
  menores no bloqueantes: normales laterales de ramas rotadas no siguen la
  rotación en `tree-mesh.ts`; normales horizontales de briznas en
  `grass-mesh.ts` que oscurecen la mata desde cámara aérea; array temporal por
  roca en `vegetation-lowpoly.ts` — irrelevante con presupuestos actuales;
  helpers de bajo nivel expuestos por `export *` de `procedural/index.ts`).
  Autorizado para continuar; sin excepciones de deploy/push.

## 128A-1 — Terreno por bloques (Minecraft) del Bosque (experimento del toolkit)

**Contexto (decisión del usuario, 13-ago):** 128A-1 deja de ser el destino
visual y pasa a **experimento reutilizable** dentro del nuevo enfoque del
Bosque (motor/toolkit propio, 138A-1): su mesher de bloques y su panel se
reutilizan como modo `bloques` del comparador visual. El estilo final (low poly
suave vs bloques) lo decide el usuario probando, no por hipótesis.

### Qué

1. **Refactor a datos puros + adaptador Three** de la isla Curved Island:
   `game-block-heightmap.ts` (alturas enteras, isla ovalada con falloff,
   relajación `diff ≤ 1` y poda de islotes), `game-block-palette.ts` (colores
   del mundo), `game-block-mesher.ts` (caras top/side con AO y jitter),
   `game-curved-island.ts` (adaptador: terreno, props, agua, lluvia, picking y
   highlight) y `game-curved-island-panel.ts` (panel temporal: curva del mundo,
   lluvia, props, cámara sigue y regenerar). `game-world-bend.ts` se conserva.
2. **Integración aditiva** en `game-playable-scene.ts`: la isla 128A-1 se
   monta como override visual del fixture, el comparador procedural
   (`game-procedural-comparator.ts`, 138A-1) se monta oculto con modos
   `bloques`/`suave`, y el panel conecta `setTerrainMode`/`setTerrainMetrics`/
   `setPick` nullable sin cambiar el comportamiento por defecto.
3. **Fix hallado en validación visual real:** `toIndexedGeometry` pasaba
   `number[]` (indices de la vegetación low-poly) directo a
   `THREE.BufferAttribute`, que exige `TypedArray`; la escena no montaba
   (`este dispositivo no pudo iniciar el fixture 3d`). Se normaliza con
   `new Uint32Array(data.indices)` en el adaptador. Tras el fix, reload con
   logs frescos: **cero errores/warnings de consola** y comparador operativo
   (modo suave: tris 2914 · vértices 1536 · props 123 · draw calls 10).

### Archivos

- `frontend/src/features/desktop/apps/game-playable/game-block-{heightmap,mesher,palette}.ts` + `{heightmap,mesher}.test.ts` (5 archivos nuevos).
- `frontend/src/features/desktop/apps/game-playable/game-curved-island.ts`, `game-curved-island-panel.ts`, `game-world-bend.ts` (nuevos).
- `frontend/src/features/desktop/apps/game-playable/game-playable-scene.ts`, `game-procedural-comparator.ts`, `game-playable-visual-cache.ts` (+test), `game-shared/forest-models.ts` y `frontend/src/styles/desktop/desktop-game-playable.css`.
- `Agente/planes/plan-terreno-bloques-bosque-minecraft-2026-08-12.md` (→ `Agente/planes/completados/`).
- `roadmap.md` (entrada retirada de Pendientes; queda sin commitear junto a edits ajenos).

### Gotchas

- **TypedArray en Three:** `THREE.BufferAttribute` rechaza arrays planos;
  `Float32BufferAttribute` los normaliza pero `setIndex` no. El toolkit suave
  entrega `indices: number[]` (datos puros) y el adaptador debe convertirlos.
  El gate no detecta esto: solo la validación real en navegador lo destapa.
- **La escena requiere catálogo de personajes:** sin backend, la app no monta
  WebGL; para validar se levantó un stub local temporal en `:3000`
  (`/api/game/characters` → forest-scout, `/api/game/profile` → 401, resto 404),
  sin tocar el repo, y se retiró al cerrar.
- **Trabajo en serie sin worktree (patrón 138A-1/138A-2):** el commit de
  implementación incluye solo archivos propios (sin `git add .`); roadmap y
  docs ajenos quedan sin commitear.

### Sentinel / gate

- `npm run gate:check -- 128A-1`: **PASS** (incremental, 32 archivos) —
  sentinel PASS (0 errores, 5 warnings), varsense PASS (0 errores, 2 warnings),
  frontend PASS (type-check), docs PASS. Clasificación de los 7 findings:
  - **Heredados:** `boton--active`/`css-adhoc-button-style` (líneas 200/373,
    origen 297A-64) y `limite-lineas` de `game-playable-scene.ts` (498) y
    `game-playable-visual-cache.ts` (313) — ya excedían antes de este bloque.
  - **Nuevos:** `limite-lineas` en `game-curved-island.ts` (359 efectivas,
    archivo nuevo — deuda programada: dividir en `game-curved-water.ts` +
    `game-curved-rain.ts`); `dom-access-outside-platform` en
    `game-curved-island.ts:93` (excepción aceptada: el canvas se usa como
    textura de shore mask dentro del adaptador Three, no como UI; anotado en
    prevención); `claseHuerfana juegoPanelTerreno--cerrado` (línea 302,
    **falso positivo**: la clase se togglea dinámicamente con
    `classList.toggle` en `game-curved-island-panel.ts:49`).
- Verificación base con artefacto: `npm --prefix frontend run test:file --`
  (12 archivos: game-block-heightmap, game-block-mesher,
  game-playable-visual-cache, game-playable y 8 del toolkit procedural) →
  **12 files / 47 tests PASS** (vitest 4.1.10, 29.1s); `npm --prefix frontend
  run type-check` limpio. Nota: la etapa frontend del gate local-light solo
  ejecuta type-check; la suite vitest es evidencia complementaria (no hay
  test-report en `.quality-reports` porque la carpeta está ignorada).
- Validación visual real en `/forest-playable` (navegador in-app): canvas
  WebGL montado, panel con Actual/Bloques/Suave operativo, métricas del
  comparador visibles, sin errores de shader tras el fix.
- Sentinel 0.7.4 (`0349485c…`), VarSense 2.2.1 (`88f281f…`), rama `wandorius`.

### GLORY

- Commit explícito de implementación + commit documental de cierre (plan a
  completados y completada).
- Revisión `sentinel_inspector`: **OK CON OBSERVACIONES, ACCION: ninguna**
  (gate final PASS reproducible tras los commits, alcance incremental
  coherente, commits explícitos sin barrer roadmap ni hunks ajenos, fix
  documentado en `game-procedural-comparator.ts`; observaciones no bloqueantes:
  takeover del consumidor pendiente de release tras la revisión, 7 warnings
  de deuda, roadmap sin commitear por patrón, metadata histórica ajena).
- Revisión `supervisor_reviewer`: **APROBADO CON RESERVAS — autorizado para
  continuar** (SOLID bien en datos puros y comparador; reservas de deuda
  declarada, no defecto de corrección: `game-curved-island.ts` excede 300
  líneas y mezcla agua/shore/lluvia con el adaptador — dividir en módulos;
  `dom-access-outside-platform` nuevo con excepción documentada; completada
  ahora clasifica los warnings nuevos vs heredados; artefacto de vitest 47/47
  anexado arriba; botones ad-hoc del panel temporal aceptados como excepción
  del host WebGL). Sin excepciones de deploy/push; sin SSH directo.

## 138A-3 — Toolkit de agua y lluvia + split de la isla curva

**Contexto:** continuación del nuevo enfoque del Bosque (138A-1/138A-2). El
revisor del cierre 128A-1 declaró la deuda de `game-curved-island.ts` (399
líneas, mezcla terreno/agua/shore/lluvia) y el usuario quiere probar el estilo
Genshin-like 1:1 contra la referencia Curved Island: el comparador debe mostrar
el MISMO agua real en ambos modos (`bloques`/`suave`), no agua toon estática.

### Qué

1. **Toolkit puro `water-mesh.ts`** (`buildWaterMeshData`): grid indexado en el
   plano XZ con y=0, z decreciente con j, índices con normal +Y (misma
   orientación que el `PlaneGeometry` rotado de 128A-1), UV normalizados,
   `wavePhase` determinista por vértice vía `hash2` y validaciones fail-closed
   (ancho/profundo > 0, segmentos 1..=256). `wavePhase` queda reservada para
   variantes de oleaje por vértice; el shader actual anima por posición.
2. **Toolkit puro `rain-mesh.ts`** (`buildRainStreakData`): streaks
   deterministas (ángulo, radio con raíz cuadrada, phase en [0, span), longitud
   por gota) con presupuesto máximo 4096; sustituye los `Math.random()`.
3. **Adaptador `game-curved-water.ts`** (205 líneas): `mountCurvedWater` con
   shore mask (blur 3 pasadas), shader de costa idéntico al original
   (deep/shallow/foam/niebla), `setShore`/`update`/`setVisible`/`dispose` y
   opción `addToScene: false` para consumidores que re-parentan el mesh
   (el comparador lo aloja en `world` para que siga el bend del mundo).
4. **Adaptador `game-curved-rain.ts`** (100 líneas): `mountCurvedRain` con
   streaks desde el toolkit (1100 gotas, área 26, span 21, top 15) y
   `setAnchor`/`setTime`/`setAmount`/`setVisible`/`dispose`.
5. **`game-procedural-geometry.ts`** (33 líneas): `toGeometry`/
   `toIndexedGeometry` compartidos (antes duplicados en isla y comparador).
6. **`game-curved-island.ts` refactorizado: 399 → 221 líneas**, delegando
   agua y lluvia a los nuevos módulos; `regenerate` refresca la shore y
   `setRain`/`setVisible`/`dispose` delegan. Sin cambio visual.
7. **Comparador `game-procedural-comparator.ts` (310 → 286 líneas):** usa
   `mountCurvedWater` con `{width: WIDTH, depth: DEPTH, meshScale:
   WATER_MESH_SCALE (2.4), addToScene:false, waterY:-0.12, seed}` — el océano
   que rodea la isla se conserva (anillo 2.4×); shore en `rebuild()` desde
   `heights >= waterLevel`; `update(t)` anima el agua; `dispose` libera.
   `WorldBendLike` reemplazado por el tipo real `WorldBend`.

### Archivos

- `frontend/src/features/game-core/procedural/{water-mesh,rain-mesh}.ts` +
  `{water-mesh,rain-mesh}.test.ts` y exports en `procedural/index.ts`.
- `frontend/src/features/desktop/apps/game-playable/game-curved-{water,rain}.ts`,
  `game-curved-island.ts`, `game-procedural-comparator.ts` y
  `game-procedural-geometry.ts`.
- `Agente/planes/plan-toolkit-agua-lluvia-2026-08-13.md` (→
  `Agente/planes/completados/`).
- `roadmap.md` (entrada 138A-3 cerrada; mantiene el taskId que exige el
  analyzer docs).

### Gotchas

- **Enlace roto en roadmap:** la línea 17 referenciaba
  `Agente/planes/plan-terreno-bloques-bosque-minecraft-2026-08-12.md` (no
  existe: fue archivado); corregido a `Agente/planes/completados/...`. Era lo
  que hacía fallar el analyzer docs; tras el fix el gate quedó PASS.
- **Re-parentado del agua en el comparador:** `mountCurvedWater` agrega el
  mesh a la escena por defecto; el comparador lo re-parenta a `world` (bend).
  Se expone `addToScene: false` para hacerlo explícito y `dispose` retira el
  mesh de su padre (no solo de la escena). Sin impacto funcional.
- **`aWavePhase` sin consumidor:** atributo preparado para variantes de oleaje
  por vértice; anotado en el toolkit y en el adaptador (no es deuda viva).
- **El gate local-light solo ejecuta type-check en frontend:** la suite vitest
  es evidencia complementaria; artefacto: 109 archivos / 780 tests PASS.
- **Validación visual:** el usuario abrió `/forest-playable` en el navegador
  in-app (1 tab) para probar; no hubo captura automatizada (el runtime
  node_repl del entorno no ejecuta). Ajustes visuales posteriores los hará el
  usuario.
- **Edits ajenos:** por instrucción del usuario (13-ago: "todos los edit ajenos
  terminalos y subelos") se commitean y pushean junto al cierre, en commit
  separado del de implementación (sin `git add .`).

### Sentinel / gate

- `npm run gate:check -- 138A-3`: **PASS** (incremental, 28 archivos) —
  sentinel PASS (0 errores, 2 warnings `dom-access-outside-platform` en
  `game-curved-water.ts`, excepción pre-aceptada de 128A-1), varsense PASS,
  frontend PASS (type-check), docs PASS. Límites de líneas cumplidos: isla
  221, agua 205, lluvia 100, comparador 286, geometry 33, water-mesh 106,
  rain-mesh 78.
- Verificación base: `npm --prefix frontend run type-check` limpio; vitest
  completo **109 archivos / 780 tests PASS** (incluye 6 tests de `water-mesh`
  y 5 de `rain-mesh`).
- Sentinel 0.7.4 (`0349485c…`), VarSense 2.2.1 (`88f281f…`), rama `wandorius`.

### GLORY

- **Corrección por feedback del usuario (agua toon en modo "Actual"):**
  tras el cierre, el usuario reportó que el agua del estilo Actual seguía
  mostrando "triángulos por encima" (la malla segmentada del shader de costa
  con olas/espuma, no los modos Bloques/Suave). `mountCurvedWater` pasó a
  montar el MISMO plano toon estático que el comparador restaurado
  (`MeshToonMaterial waterShallow` con rampa y `bend.apply`), y la isla dejó de
  generar/refrescar shore. Segunda corrección (`8684af12`): el plano quedó en
  32×32 con `polygonOffset` (−1,−1) y `renderOrder=1` para que el bend doble el
  interior y el agua sea visible también en el modo Suave (con 1×1 el interior
  interpolado quedaba decenas de unidades bajo el fondo marino). La
  configuración del plano toon se extrajo al helper compartido
  `game-toon-water.ts` para que comparador e isla no vuelvan a divergir.
  Archivos: `game-curved-water.ts`, `game-procedural-comparator.ts`,
  `game-curved-island.ts` y `game-toon-water.ts`. Evidencia: type-check limpio
  y vitest 58/58 PASS en 7 archivos (13.77s:
  `node scripts/quality/run-frontend-tests.mjs`); gate `npm run gate:check --
  138A-3` reproducido en el bloque de corrección.
- Commit explícito de implementación + commit documental de cierre + commit de
  edits ajenos (roadmaps cancelados, planes/prevenciones archivados y
  referencia visual del usuario), todo pusheado a `origin/wandorius` por
  instrucción del usuario.
- Revisión `supervisor_reviewer`: **APROBADO CON RESERVAS — autorizado para
  continuar** (cierre documental ejecutado en este bloque; vitest 109/780 como
  evidencia; `addToScene: false` explícito; `aWavePhase` anotada como
  preparación para variantes; sin defecto de corrección).
- Revisión `sentinel_inspector`: **OK CON OBSERVACIONES, ACCION: ninguna**
  (gate final PASS reproducible post-commit, 29 archivos, 15.3s, exit 0;
  alcance incremental coherente; commits explícitos selectivos en HEAD
  `9e89a0ed`/`36e82aa6`/`610c945f`; árbol limpio; límites de líneas
  verificados ≤300; observaciones no bloqueantes: claims activos por diseño
  liberados tras el veredicto, 2 warnings `dom-access-outside-platform`
  heredados del patrón isla como deuda programada, 3 invalidMetadata
  históricos ajenos en `.sentinel`). No aplica `sentinel_repair`.
