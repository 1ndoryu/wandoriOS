# Tareas completadas — 2026-08-05

## Qué

- **297A-73 — Panel de versiones de Assets 3D (frontend):** servicio admin
  (`listVersions`/`import`/`updateMetadata`/`activate` + lectura binaria del GLB
  vía fetch directo), preview 3D aislado con GLTFLoader, panel de versiones en
  `game-settings` y endpoint backend para servir el GLB a admin. Gate PASS,
  type-check, 142 tests frontend y build PASS. (El código se commiteó en
  `91b5f13f` como parcial; el gate y el registro se cerraron hoy.)
- **297A-74 — Cierre Fase 4 (culling avanzado, batching por materiales y
  medición física de GPU/memoria):** `maxDistance` circular en
  `MapChunkCache.select`; `groupMeshesByMaterial` fusiona meshes del prototipo
  con misma geometría+material en un solo `InstancedMesh`; `game-gpu-probe.ts`
  lee identidad GPU, tiempo de frame con `EXT_disjoint_timer_query` y bytes
  estimados; la escena publica `data-gpu-*`/`data-batch-*`. Gate PASS,
  type-check, 196 tests frontend del juego y build PASS.
- **297A-75 — Fase 8 (dos salas concurrentes, métricas agregadas y runbook):**
  `GameRoomState` multi-sala por `map.map_version()`; `GameRoomMetrics` con
  contadores atómicos y `GET /api/game/metrics` público agregado; runbook de
  rollback de mapa/assets. Gate PASS (local-light; full diferido por cooldown),
  9/9 unit de `game_room`, 8/8 TCP, 51 tests backend de regresión y 665 tests
  frontend PASS.
- **Validación visual del fixture GAME-01 (cierre):** backend actualizado
  compilado y ejecutado en localhost:3000 con la BD de rama (todas las rutas
  del juego: `characters`, `metrics` → 200); Vite del proyecto en :5174.
  En navegador: `/forest-playable` monta el canvas del fixture con WebGL2,
  GPU detectada (`Intel Iris Xe` vía ANGLE/D3D11), datasets
  `character-id=forest-scout`, `character-tone=ink`, `player-name=Jugador`,
  `webgl-kind=webgl2`; movimiento funcional (pad ↑←↓→) sin errores de
  runtime; el 401 de `/api/game/profile` y el 404 de `/api/game/maps/bosque`
  son esperados sin sesión/mapa activo.

## 058A-1 — Fix grid del escritorio: iconos fuera del viewport al redimensionar + distribución `space-between`/center

- **Qué:** al encoger la ventana de escritorio, el icono más a la izquierda
  (Notas) desaparecía porque el reflow no lo recolocaba: `getGridMetrics.columns`
  contaba los tracks de `gridTemplateColumns`, que incluyen el track implícito
  que el propio icono crea al estar posicionado fuera del área visible con
  `auto-fill` (inflando el conteo), así que `reflowPositions` creía que cabía y
  `overflow: hidden` lo cortaba. Fix raíz: las columnas visibles se calculan por
  geometría real (`floor((width + gap) / (cellWidth + gap))`). Además se
  aplicaron los estilos pedidos en `.desktop-icon-grid`
  (`justify-content`/`align-content: space-between`, `align-items`/`justify-items:
  center`) y se replicó la distribución vertical en `getGridMetrics`/`getCellAt`
  con un `rowGapEffective` (el sobrante vertical repartido entre filas) para que
  el snap-grid, el highlight de arrastre y el debug overlay sigan exactos.
- **Archivos:** `frontend/src/styles/desktop/desktop-shell.css`,
  `frontend/src/features/desktop/utils/icon-grid.ts`,
  `frontend/src/features/desktop/utils/icon-grid.test.ts`,
  `frontend/src/features/desktop/utils/icon-grid-placement.test.ts`,
  `frontend/src/features/desktop/workspace-icon-grid.ts`.
- **Gotchas:** `gridTemplateColumns` del computed style incluye tracks implícitos
  creados por items posicionados fuera del área visible — nunca usarlo para
  contar columnas con `auto-fill`; usar geometría real del contenedor. El reflow
  preserva la columna tras recolocar (Notas pasa de col 12 a 7 y permanece ahí
  al volver a 1440px), que es el comportamiento esperado.
- **Sentinel:** no aplica (cambio CSS/TS validado por stack; gate bloqueado por
  el checkout de 028A-6).
- **GLORY:** no aplica (lógica específica del escritorio wandori.us).

## 058A-2 — Highlight del drag desfasado + crash de deep-links en el Finder

- **Qué:** (1) el placeholder del drag (`.desktop-icon-drop-target`) quedaba
  desfasado verticalmente tras aplicar `align-content: space-between`:
  `positionCellHighlight` usaba `rowGap` fijo y no el `rowGapEffective` que
  `getCellAt`/debug overlay ya usaban, así que el cuadrado no coincidía con la
  celda real cuando el sobrante vertical se reparte entre filas. (2) El clic en
  ítems del Finder crasheaba con `TypeError: Cannot read properties of null
  (reading 'length')` en `isSafeSegment`: los `publicLocator` pueden llegar con
  params `null` en runtime y `value === undefined` no los capturaba. Ahora
  `isSafeSegment` rechaza `null`/`undefined` sin lanzar (fail-closed).
- **Archivos:** `frontend/src/features/desktop/utils/icon-reorder.ts`,
  `frontend/src/features/runtime/deep-links.ts`,
  `frontend/src/features/runtime/deep-links.test.ts`.
- **Gotchas:** el `rowGapEffective` (36px vs 32px declarados con el grid a
  970px) marca la diferencia: cualquier cálculo de fila del snap-grid debe
  usarlo, no `rowGap`. El test de regresión reproduce el crash exacto
  (`parse({ slug: null })`), que antes lanzaba.
- **Sentinel:** no aplica (validado por stack; gate bloqueado por 028A-6).

## 058A-3 — Ocultar del shell los nodos sin apertura posible (sin URL pública ni visor local)

- **Qué:** los recursos del workspace con `publicLocator` roto (slug nulo) o
  inexistente aparecían en el Finder (p. ej. "Artículo de prueba" ×3, huérfanos
  del release publicado) y solo producían el aviso «Este recurso todavía no
  tiene una referencia pública disponible» al hacer doble clic. Requisito del
  usuario: «no deberían aparecer nunca cosas que no tienen url o no funcionan».
  Se añadió el helper central `canOpenNodeFromShell(node, opts)` en
  `public-resource-locator.ts` (carpetas siempre; apps solo con `refId`;
  recursos solo si hay visor de imagen local en el Finder o un
  `resolvePublicResourceTarget` válido; shortcuts igual) y se aplicó en las tres
  superficies: Finder (`finder-preview.ts` con `allowImagePreview: true`),
  escritorio (`workspace-icon-grid.ts` → `resolveActivate` devuelve `undefined`
  y el nodo queda fuera de `activableNodes`) y launcher móvil
  (`mobile-launcher.ts` → `createIconButton` no crea el botón). Se eliminaron
  los imports muertos de `showToast` en los dos grids.
- **Archivos:** `frontend/src/features/runtime/workspace/public-resource-locator.ts`,
  `frontend/src/features/runtime/workspace/public-resource-locator.test.ts`,
  `frontend/src/features/desktop/apps/finder/finder-preview.ts`,
  `frontend/src/features/desktop/workspace-icon-grid.ts`,
  `frontend/src/features/mobile/mobile-launcher.ts`.
- **Gotchas:** el sync local (`article-notas-sync.ts`) ya tenía la condición
  correcta (`published && slug`); los nodos huérfanos persisten en el release
  público publicado, así que el filtro de render es la garantía de «nunca
  aparecen». El toast de fallback se conserva como red de seguridad
  (no-silent-failures). Verificado en navegador: el Finder de Notas pasa de 6 a
  3 ítems (los "Artículo de prueba" desaparecen) y el launcher móvil no lista
  recursos huérfanos.
- **Sentinel:** no aplica (validado por stack; gate bloqueado por 028A-6).
- **GLORY:** no aplica (filtro de superficie específico de wandori.us).
- **GLORY:** no aplica.

## Archivos

- Frontend: `map-streaming-contracts.ts`, `map-streaming.ts` (+test),
  `game-playable-visual-cache.ts` (+test), `game-gpu-probe.ts` (+test),
  `game-playable-scene.ts`, `game-playable.ts`.
- Backend: `src/services/game_room.rs`, `src/handlers/game_metrics_handler.rs`,
  `src/handlers/mod.rs`, `tests/game_ws_tcp.rs`.
- Docs: `roadmap.md`, `Agente/planes/plan-juego-bosque-multijugador-2026-08-01.md`,
  `Agente/documentacion/operacion/runbook-rollback-juego-2026-08-05.md`.
- Regenerado: OpenAPI + Orval (endpoint `GET /api/game/metrics`).

## Cierre documental del plan GAME-01 (checklists con evidencia)

- **Checklists del plan:** 81 casillas sin marcar → 18, todas con justificación
  (decisiones de producto tuyas, mediciones de entorno dedicado o Fase 9 futura).
  Se marcaron con evidencia las auditorías de cierre de Fases 0/1/2/3/4/5/6/7/8,
  los ítems de Fase 1 (bocetos), Fase 2 (contratos/mensajes/restauración/vertical
  slice/glory-render), Fase 3 (apertura/cierre), Fase 6 (auditoría) y el DoD
  (12 de 16 ítems con evidencia de los commits 297A-4x/5x/6x/7x).
- **Nuevo documento:** `Agente/documentacion/producto/decisiones-pendientes-bosque-2026-08-05.md`
  consolida la Fase 0 (referencia como atmósfera, gramática visual, capturas) y
  la sección 12 del plan (salas, cromática, controles móviles, invitado,
  publicación en vivo, escalado) con recomendaciones y tabla resumen.
- **ADR actualizado:** `adr-bosque-3d-assets-terreno-2d-2026-08-01.md` marca
  relieve (alturas 0–4, 297A-67) y cámara (isométrica limitada, 297A-30/70) como
  resueltos, y documenta el estado del presupuesto: medición local existente
  (297A-46/74/75) y pendiente formal en entorno dedicado + multi-viewport.
- **Índice documental** enlaza el nuevo documento de decisiones.

## Gotchas

- El gate exige que la tarea exista en roadmap/planes/completados antes de
  ejecutarse (`preflight.assertTaskExists`); una tarea commiteada como parcial
  sin registrar no pasa.
- El entorno se reinició: `frontend/node_modules` perdió vitest/three/lucide;
  reinstalar con `npm ci` y, si Cargo falla por el exe en uso
  (`glory-backend.exe` del puerto 3000), usar `CARGO_TARGET_DIR` temporal para
  `codegen-local.mjs` en vez de matar el proceso ajeno.
- `tokio::sync::RwLock::blocking_write()` dentro de un runtime async panics;
  usar `std::sync::RwLock` para accesos cortos sin `.await` mientras se
  mantiene el lock.
- El snapshot inicial del join no pasa por `broadcast_snapshot`, así que el
  contador `snapshots_sent` arranca en el primer tick (assert `>= 1`, no `>= 2`).
- `tools/sentinel` apareció como "cambiado" en un scope por suciedad transitoria
  de otro hilo (submódulo); VarSense rechaza directorios en `--files-from`.
  Re-ejecutar el gate con el submódulo limpio resuelve el falso positivo.
- La validación visual del fixture con datasets GPU/métricas queda pendiente de
  una sesión con backend actualizado (el binario del puerto 3000 no expone las
  rutas del juego) y del full CI cuando expire el cooldown.

- **297A-76 — Reclamación invitado→cuenta (limpieza de identidad temporal):**
  `GameTicketStore::revoke_guest` (elimina la entrada del store; exige firma y
  entrada vigente; fallo no toca la identidad), login/logout expiran la cookie
  `guest_game` y el handler del ticket revoca la cookie invitada cuando viaja
  con sesión autenticada (la cuenta es la autoridad). 11/11 unit de
  `game_ticket`, 6/6 HTTP de tickets y 8/8 TCP de regresión PASS; gate
  `task:check -- 297A-76` PASS.

- **297A-77 — Presencia avanzada (characterId + resync):**
  - `GameRealtimeEntity.character_id` (bounded 64, fail-closed, camelCase) en
    el modelo y el validador TS; el snapshot de cada jugador lleva su personaje.
  - Resolución del personaje al emitir el ticket (HTTP con BD) contra
    `game_profiles`; viaja server-side en `PendingTicket`/`GameTicketClaims`
    (nunca en el token firmado; `issue()`/`issue_at()` ganan el parámetro
    `Option<&str>` con validación `is_valid_id` fail-closed).
  - `GameRoomState::join_with_character` (default `forest-scout`);
    `RoomPlayer.character_id`; el handler WS usa `claims.character_id` con
    fallback al default si falta o es inválido (sin BD en el socket).
  - Frontend: `SnapshotEntity`/`PlayerState.characterId`; preservación en
    `createWorldState`/`normalizeState` (default `forest-scout`); tono
    `ink|middle|paper` → material compartido en `createFigure`; la escena
    recrea la figura si el personaje cambia; jugador local offline con su
    personaje; fix de resync (reset de `lastSnapshotSequence` + snapshots al
    recibir `joined`).
  - Gate `task:check -- 297A-77` PASS; 12/12 unit de `game_ticket`, 19 unit de
    room/realtime/ws-handler, 8/8 TCP, 54 backend HTTP de regresión y 668
    tests frontend + type-check PASS.
  - Pendiente documentado: validación visual en navegador (backend del puerto
    3000 desactualizado) y full CI tras el cooldown.

## Sentinel / GLORY

- Sentinel: 0 errores en los gates 297A-73/74/75 (warnings de
  `console-production` preexistentes en `game-playable.ts`).
- VarSense: PASS con el índice persistente; sin clases huérfanas nuevas.
- GLORY: sin cambios; los módulos agnósticos siguen en `frontend/src/features/game-core/`.

## Gobernanza del escritorio (038A-2) + soft delete de artículos (028A-12)

- **038A-2 — Guard de nodos de sistema + contenido publicado siempre visible:**
  - Backend: `SYSTEM_NODE_IDS` (`trash`, `admin`, `settings`, `profile`,
    `about`) y `first_missing_system_node` en `release_validation.rs`
    (commiteado en HEAD); `validate_release_tree` rechaza releases sin ellos.
  - Tests: `workspace_publish.rs` con fixture de summary corregido (`about`
    canónico, nodeCount=7) + test de regresión
    `publish_rejects_tree_without_system_nodes`.
  - Frontend: `isSystemNode`/`SYSTEM_NODE_IDS` compartidos en
    `overlay-mutations.ts`; `tombstoneNode`/`tombstoneSubtree` no-op con
    `console.warn` sobre nodos de sistema; comando `workspace:trash` hidden +
    doble guardia.
  - Sync envelope: `ArticleService::update` mapea `status` → `editorial`/
    `visibility` vía `update_resource_metadata` en la misma transacción
    (`published` → ready/public; `draft` → draft/private).
  - Release efectiva server-side: `find_public_content()` en `resource_repo.rs`
    (kind article/media, ready/public/active) + `materialize_content_nodes()`
    en `workspace_svc.rs` que inyecta `nota-{id}` (bajo Notas) y `media-{id}`
    (bajo Documentos/subcarpeta por tipo) en `get_active_release` y
    `get_release_by_version`, **sin mutar la release inmutable**, idempotente.
  - Frontend (B): `publishWorkspace` skipea nodos `nota-`/`media-` al hornear;
    `activateVersion` refresca `fetchWorkspaceRelease()` tras activar.
  - Backfill BD: 3 artículos legacy `status='published'` con envelope
    `draft/private` → `ready/public/active`.
  - Validación por stack (gate bloqueado por submódulo de 028A-6):
    `cargo build` EXIT 0, `cargo build --tests` EXIT 0, frontend sin errores TS.
- **028A-12 (heredado y terminado) — Soft delete de artículos:**
  - Migración `20260805000000_028a12_article_soft_delete` (`trashed` +
    `deleted_at` + índice).
  - `ArticleRepository::delete` transaccional (marca `trashed` + soft delete
    del envelope vía `soft_delete_kind_tx`), `restore`, listado de papelera,
    `update` con conexión.
  - Endpoints: `DELETE /api/admin/articles/{id}` (204), `GET
    /api/admin/articles/trashed`, `POST /api/admin/articles/{id}/restore`.
  - `tests/article_soft_delete.rs`: ciclo crear → delete → papelera → restore
    → envelope vuelve a active.

## Archivos del bloque

- Frontend: `frontend/src/features/runtime/workspace/overlay-mutations.ts`,
  `frontend/src/features/runtime/commands/workspace-node-commands.ts`,
  `frontend/src/features/runtime/workspace/workspace-store.ts`,
  `frontend/src/pages/admin-workspace.ts`.
- Backend: `src/models/resource.rs`, `src/repositories/resource_repo.rs`,
  `src/services/workspace_svc.rs`, `src/services/article.rs`,
  `src/handlers/articles.rs`, `src/models/article.rs`,
  `src/repositories/article.rs`, `tests/workspace_publish.rs`,
  `tests/article_soft_delete.rs`.
- Migraciones: `migrations/20260805000000_028a12_article_soft_delete.up.sql`
  y `.down.sql`.

## Gotchas del bloque

- El gate `task:check -- 038A-2` no pudo ejecutarse: submódulo
  `tools/sentinel` con checkout modificado sin patch declarado (hilo 028A-6,
  otro frente). `cargo check`/`test` directos bloqueados por el guard (exit
  78); `cargo build` y `cargo build --tests` NO están bloqueados y pasan.
- El `status='published'` legacy NO sincronizaba el envelope de `resources`
  hasta la Fase 4; el backfill corrigió los existentes. Artículos sin fila en
  `articles` (slug NULL) materializan `publicLocator` con slug vacío.

## 058A-4 — Selección múltiple con mouse (estilo Windows) en escritorio y Finder

- **Qué:** selección múltiple con el mouse en el escritorio y el Finder, estilo
  Windows: Ctrl/Cmd+clic alterna, Shift+clic extiende rango (orden visible del
  grid), clic simple reemplaza y un clic sobre un ítem ya seleccionado conserva
  la selección (permite arrastrar el grupo). Rubber band desde el fondo en
  ambas superficies con feedback provisional por clase CSS sin tocar el store
  hasta soltar (aditiva con Ctrl/Cmd). Drag de grupo: `buildGroupPlacementMoves`
  en el escritorio mantiene el offset relativo del plan aplicado a cada
  seleccionado; en el Finder `onGroupDrop` mueve todos los capturados al target.
  Menú contextual multi: sobre una selección actúa sobre todos los targets
  (Eliminar multi con confirmación única, copiar/cortar multi); `workspace:open`
  y `workspace:rename` quedan ocultos con multi-selección. Refactor de
  rendimiento: el Finder aplica las clases de selección a los ítems existentes
  (`itemElements` Map) sin reconstruir todo el grid en cada cambio del store.
  **Fix del scroll del Finder (petición del usuario):** la banda sin recorte
  creaba scrollable overflow en `.desktop-window__body` (overflow:auto) al
  arrastrar más allá de los bordes; la banda ahora se recorta a
  `clientWidth/clientHeight` del contenedor y `onPointerMove` llama
  `e.preventDefault()`. Verificado en navegador (Playwright, 1440×900 y
  390×844): fix de scroll confirmado (banda recortada a la ventana, scroll
  0/0), banda selecciona 4/4 en Finder y 14/14 en escritorio, Ctrl+clic togglea,
  Shift+clic extiende, drag de grupo con delta idéntico (−217/+117 en los tres),
  menú multi con Eliminar (nodos no-sistema) y sin Abrir/Renombrar, y regresión
  móvil sin romper. Tests: 8 de `selection-band-math` (intersección de la banda)
  + 5 de `selection-store` (selectMany) = 13/13 PASS; `tsc --noEmit` sin
  output; `get_errors` limpio en los 5 archivos clave.
- **Archivos:** `frontend/src/features/desktop/utils/selection-band.ts` (nuevo),
  `selection-band-math.ts` (nuevo), `selection-band.test.ts` (nuevo),
  `icon-group-drag.ts` (nuevo), `debug-grid-overlay.ts` (nuevo),
  `frontend/src/features/desktop/workspace-icon-grid.ts`,
  `frontend/src/features/desktop/apps/finder/finder-preview.ts`,
  `frontend/src/features/desktop/utils/icon-drag.ts`,
  `frontend/src/features/runtime/commands/workspace-node-commands.ts`,
  `frontend/src/features/runtime/selection-store.ts`,
  `frontend/src/features/runtime/selection-store.test.ts` (nuevo),
  `frontend/src/styles/desktop/desktop-shell.css`,
  `frontend/src/styles/desktop/desktop-apps.css`, `roadmap.md`,
  `Agente/planes/plan-seleccion-multiple-2026-08-05.md` (movido a completados).
- **Gotchas:** (1) la banda sin clamping crea scrollable overflow en el scroll
  container del Finder — recortar a `clientWidth/clientHeight` + `preventDefault`
  en `onPointerMove`; el bug afectaba también al escritorio (util compartida) y
  el fix beneficia a ambos. (2) Los límites de Sentinel (util ≤150 líneas,
  componente ≤300) obligaron a dividir `selection-band.ts` en
  `selection-band-math.ts` y `workspace-icon-grid.ts` en `icon-group-drag.ts` +
  `debug-grid-overlay.ts`; al extraer se perdió `MOVE_THRESHOLD_PX` (restaurado).
  (3) En la verificación con Playwright, cerrar ventanas con `w.remove()` rompe
  el WindowManager (estado stale) — cerrar vía botones con `.click()`. (4)
  Colisiones del grupo contra otros iconos del snap-grid no resueltas (mejora
  futura documentada en el plan). (5) El flujo destructivo de Eliminar multi no
  se probó en navegador (habría tombstoneado datos reales); la lógica está
  cubierta por `isAvailable` y la confirmación única con «N elementos».
- **Sentinel:** no aplica (validado por stack: tsc + vitest + get_errors + navegador; gate bloqueado por el checkout de 028A-6).
- **GLORY:** no aplica (interacción específica del escritorio wandori.us).

## 028A-16 — Auditoría del uso de excepciones del guard (prevención cooldown) + 028A-8 Fase 0/4 (métricas del orquestador)

- **Qué (028A-16):** la prevención `prevencion-cooldown-guard-2026-08-02.md`
  pedía trazar cada activación de `--allow-heavy`/`GLORY_QUALITY_ALLOW_HEAVY`/
  `GLORY_HEAVY_RUN_TOKEN` y exigir motivo. Ahora `logHeavyOverride` escribe
  `.quality-reports/heavy-overrides.log` (JSONL: timestamp, source flag/env/token,
  comando completo, cwd, PID, motivo, granted) por cada intento, concedido o
  denegado, y nunca bloquea la decisión (fallo de escritura solo a stderr).
  `inspectHeavyRun`/`acquireHeavyRun` rechazan una excepción manual sin motivo
  (`heavy-reason-required`) antes de consultar el cooldown; el motivo llega por
  `--heavy-reason "<motivo>"` o `GLORY_HEAVY_RUN_REASON`; el shim `--execute-cargo`
  también lo propaga. El reporte del gate expone `heavyOverride` (JSON) y la
  línea `OVERRIDE` (Markdown/compacto) con source y motivo.
- **Qué (028A-8 Fase 0/4, orquestador):** el reporte ahora distingue la razón de
  invalidación de caché por etapa (`probeCachedPass`: no-entry /
  fingerprint-mismatch / not-pass / fresh / ci) en `cache.mjs`/`task-check.mjs`;
  `runVarsense` propaga las métricas del CLI (filesAnalyzed/filesReused/
  cacheHitRate/peakRssMb) al reporte; y `npm run quality:profile` (nuevo
  `quality-profile.mjs`, alias temporal de `sentinel profile`) lee los últimos
  `latest.json` de la rama y calcula p50/p95 por etapa y total sin ejecutar
  validaciones. Perfil real: sentinel hit 8/8, varsense 4/4, total p95 19273ms.
- **Archivos:** `scripts/quality/heavy-run-guard.mjs`, `scripts/quality/args.mjs`,
  `scripts/quality/task-check.mjs`, `scripts/quality/reporter.mjs`,
  `scripts/quality/cache.mjs`, `scripts/quality/adapters/varsense.mjs`,
  `scripts/quality/quality-profile.mjs` (nuevo), tests: `heavy-run-guard`,
  `args`, `cache`, `reporter`, `quality-profile` (nuevo), `package.json`
  (script `quality:profile`), `roadmap.md`, `roadmap-sentinel.md`,
  `Agente/prevencion/completados/prevencion-cooldown-guard-2026-08-02.md`.
- **Gotchas:** los tests existentes de `acquireHeavyRun` con `allowHeavy` sin
  motivo se actualizaron a `heavyReason` (la excepción ahora lo exige). El
  percentil es nearest-rank (en muestras pequeñas p95 tiende al máximo, que es
  justo lo que los presupuestos vigilan). `quality:profile` es diagnóstico, no
  gate: nunca decide PASS/FAIL.
- **Sentinel:** `quality:test` 156/156 PASS (9 tests nuevos); `task:check --
  028A-16` PASS en local-light (full diferido por cooldown del guard, esperado).
- **GLORY:** el orquestador sigue agnóstico (sin rutas de wandori.us en el core);
  el log de overrides y `quality:profile` son del proyecto.

## 018A-97 — Enforcement de tomas de tarea (bloqueo, heartbeat y banner)

- **Qué:** el registro de tomas solo informaba: un agente podía cerrar la tarea de
  otro sin que el gate se lo impidiera. Ahora `task:check` bloquea (exit 78) el
  cierre de una tarea tomada por otro agente activo salvo `--allow-foreign`
  explícito; la toma propia se renueva en cada gate (heartbeat con
  compare-and-write en `task-takeover.mjs` para no pisar un re-toma ajeno, mismo
  patrón que `takeTask` sobre stale); y cualquier `task:check`, `run-with-db` o
  `glory-dev` muestra un banner `EN CURSO` por cada toma ajena activa, no solo la
  tarea objetivo. `foreignTakeoverDecision` degrada a “sin información” ante
  registro ilegible y nunca convierte el gate en SETUP ERROR.
- **Archivos:** `scripts/quality/task-takeover.mjs` (`touchTakeover`,
  `foreignTakeoverDecision`, `listActiveForeignTakeovers`), `scripts/quality/
  task-check.mjs`, `scripts/quality/args.mjs` (`--allow-foreign`),
  `scripts/run-with-db.mjs`, tests `scripts/quality/tests/task-takeover.test.mjs`
  (3 nuevos, 8/8), `AGENTS.md` §6, `roadmap-sentinel.md`.
- **Gotchas:** sin `GLORY_AGENT_ID=<agente>` el gate usa el hostname y no
  reconoce la toma propia (muestra “tomada por otro”); la lectura corrupta del
  registro degrada a “sin información” y nunca bloquea. El TTL de 6 h es un
  recordatorio de “olvidada”, no un plazo real: el heartbeat renueva en cada gate.
- **Sentinel:** `quality:test` 210/210 PASS (3 tests nuevos) y flujo real
  take/status/conflicto/check-bloqueado(78)/check-allow-foreign/heartbeat/release
  verificado en vivo. `task:check -- 018A-97` sobre HEAD (06-ago) queda DIFERIDO
  por el falso positivo conocido del submódulo `tools/sentinel` sucio (archivos
  de otro hilo, 028A-6 Fase 5): el lockfile rechaza checkouts modificados por
  diseño; la evidencia del tramo es `quality:test` 210/210 y el gate previo
  (23:47 UTC, pre-commit) PASS.
- **GLORY:** coordinación local del checkout compartido (`.quality-reports/
  task-takeover/`, ignorado por git); nunca se commitea.

